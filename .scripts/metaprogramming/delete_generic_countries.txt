#Copy and execute this command in the explorer window of a country that will NOT get deleted

every_country = {
    if = {
        limit = {
            		NOT = { has_variable = dahae_mission }
		OR = {
		tag = DHE
		tag = PRN
		tag = ZNT
		tag = PSS
		tag = SA4
		tag = SCA
		}
		has_civil_war = no
		war = no
        }
        set_variable = { name = debug_mission_1 value = yes }
    }

    else_if = {
        limit = {
            		#is_tribal = yes
		tag = IGK
		#has_variable = can_igk_mission
		NOT = {
		has_variable = reform_cooldown
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_2 value = yes }
    }

    else_if = {
        limit = {
            		not = { has_variable = albion_mission }
		has_civil_war = no
		OR = {
		tag = AIO
		tag = HIB
		tag = CCC
		tag = BRA
		AND = {
		country_culture_group = britannic
		any_owned_province = {
		count >= 50
		OR =  {
		is_in_region = caledonia_region
		is_in_region = britain_region
		}
		}
		}
		}
        }
        set_variable = { name = debug_mission_3 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = secure_venetia_mission }
		has_civil_war = no
		AND = {
		OR = {
		primary_culture = venetic
		primary_culture = histrian
		}
		OR = {
		tag = VNT
		ROOT = {
		has_variable = venetic_country_that_formed_veneto
		}
		}
		}
		any_owned_province = {
		count >= 1
		is_in_area = venetia_area
		}
        }
        set_variable = { name = debug_mission_4 value = yes }
    }

    else_if = {
        limit = {
            		tag = ALB
		NOT = { has_variable = mission_cooldown_var }
        }
        set_variable = { name = debug_mission_5 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		tag = ANP
        }
        set_variable = { name = debug_mission_6 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = antigonid_mission_01_vision }
		NOT = { has_variable = antigonus_death }
		has_dlc = "Heirs of Alexander"
		OR = {
		tag = PRY
		has_variable = me_ant_start_flag
		}
		is_monarchy = yes
		has_civil_war = no
		NOT = { has_global_variable = antigonus_the_failure }
        }
        set_variable = { name = debug_mission_7 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = antigonid_mission_02_aspiration }
		has_dlc = "Heirs of Alexander"
		has_global_variable = antigonus_the_failure
		OR = {
		tag = PRY
		has_variable = me_ant_start_flag
		}
		is_monarchy = yes
		has_civil_war = no
        }
        set_variable = { name = debug_mission_8 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = antigonid_mission_03_birthright }
		has_dlc = "Heirs of Alexander"
		AND = {
		OR = {
		tag = PRY
		has_variable = me_ant_start_flag
		AND = {
		exists = current_ruler
		current_ruler = { has_trait = antigonids }
		}
		}
		has_global_variable = antigonus_the_failure
		has_completed_mission = antigonid_02_aspiration
		}
		is_monarchy = yes
		has_civil_war = no
        }
        set_variable = { name = debug_mission_9 value = yes }
    }

    else_if = {
        limit = {
            		tag = ARM
		NOT = { has_variable = armenia_mission_cooldown }
        }
        set_variable = { name = debug_mission_10 value = yes }
    }

    else_if = {
        limit = {
            		tag = AVN
		NOT = { has_variable = mission_cooldown_var }
        }
        set_variable = { name = debug_mission_11 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = athens_mission_01_foreign_rulership }
		OR = {
		tag = ATH
		has_variable = ath_unique_mech
		}
		has_dlc = "Magna Graecia"
		is_subject = yes
		exists = overlord
		num_of_families > 2
		is_republic = yes
		has_civil_war = no
        }
        set_variable = { name = debug_mission_12 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = athenian_mission_02_attica }
		has_dlc = "Magna Graecia"
		OR = {
		tag = ATH
		has_variable = ath_unique_mech
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_13 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = athenian_mission_03_political }
		has_dlc = "Magna Graecia"
		OR = {
		tag = ATH
		has_variable = ath_unique_mech
		}
		is_republic = yes
		has_completed_mission = athenian_mission_02_attica
		has_civil_war = no
        }
        set_variable = { name = debug_mission_14 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = athenian_mission_04_delian_league }
		has_dlc = "Magna Graecia"
		OR = {
		tag = ATH
		has_variable = ath_unique_mech
		}
		has_completed_mission = athenian_mission_02_attica
		has_civil_war = no
        }
        set_variable = { name = debug_mission_15 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		tag = BPK
        }
        set_variable = { name = debug_mission_16 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = carthaginian_mission_01_invasion }
		has_dlc = "The Punic Wars"
		tag = CAR
		NOT = {
		has_variable = carthage_home_development_completed
		}
		rank >= regional_power
		is_ai = no
        }
        set_variable = { name = debug_mission_17 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = carthaginian_mission_01_africa }
		has_dlc = "The Punic Wars"
		primary_culture = carthaginian
		NOT = {
		has_variable = africa_expansion_completed
		}
		any_owned_province = {
		count > 10
		OR = {
		is_in_region = africa_region
		is_in_region = numidia_region
		is_in_region = mauretainia_region
		is_in_region = atlas_region
		}
		}
		rank >= regional_power
		has_civil_war = no
        }
        set_variable = { name = debug_mission_18 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = carthaginian_mission_02_sicily }
		has_dlc = "The Punic Wars"
		primary_culture = carthaginian
		any_owned_province = {
		is_in_area = west_sicily_area
		}
		#		area:east_sicily_area = {
		#			any_area_province = {
		#				NOT = { owned_or_subject_owned = root }
		#			}
		#		}
		#		area:syracuse_area = {
		#			any_area_province = {
		#				NOT = { owned_or_subject_owned = root }
		#			}
		#		}
		#		c:SYR = {
		#			num_of_cities >= 1
		#		}
		OR = {
		rank = regional_power
		rank = major_power
		rank = great_power
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_19 value = yes }
    }

    else_if = {
        limit = {
            		has_variable = num_of_completed_missions
		var:num_of_completed_missions >= 1
		#Increase chance AI can actually walk to Iberia
		trigger_if = {
		limit = { is_ai = yes }
		has_completed_mission = carthaginian_africa_expansion_mission
		}
		NOT = { has_variable = "carthaginian_mission_03_iberia_cooldown" }
		has_dlc = "The Punic Wars"
		primary_culture = carthaginian
		any_owned_province = {
		OR = {
		is_in_region = contestania_region
		is_in_region = baetica_region
		}
		}
		#        area:mastiania_area = {
		#            any_area_province = {
		#                NOT = { owned_or_subject_owned = root }
		#            }
		#        }
		#        area:contestania_meridionalis_area = {
		#            any_area_province = {
		#                NOT = { owned_or_subject_owned = root }
		#            }
		#        }
		#        area:malacita_area = {
		#            any_area_province = {
		#                NOT = { owned_or_subject_owned = root }
		#            }
		#        }
		OR = {
		rank = major_power
		rank = great_power
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_20 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = carthaginian_mission_04_magna_graecia }
		has_dlc = "The Punic Wars"
		primary_culture = carthaginian
		OR = {
		has_completed_mission = carthaginian_sicily_expansion_mission
		any_owned_province = {
		is_in_region = magna_graecia_region
		NOR = {
		is_in_area = west_sicily_area
		is_in_area = east_sicily_area
		is_in_area = syracuse_area
		}
		}
		}
		any_country = {
		count >= 2
		country_culture_group = hellenic
		any_owned_province = {
		is_in_region = magna_graecia_region
		NOR = {
		is_in_area = west_sicily_area
		is_in_area = east_sicily_area
		is_in_area = syracuse_area
		}
		}
		}
		area:croton_area = {
		any_area_province = {
		NOT = { owned_or_subject_owned = root }
		}
		}
		area:heraclea_area = {
		any_area_province = {
		NOT = { owned_or_subject_owned = root }
		}
		}
		area:tarentum_area = {
		any_area_province = {
		NOT = { owned_or_subject_owned = root }
		}
		}
		OR = {
		rank = major_power
		rank = great_power
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_21 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = carthaginian_mission_05_bomilcar }
		has_dlc = "The Punic Wars"
		tag = CAR
		is_republic = yes
		current_date < 465.1.1
		has_civil_war = no
        }
        set_variable = { name = debug_mission_22 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = carthaginian_mission_06_rome }
		has_dlc = "The Punic Wars"
		tag = CAR
		exists = c:ROM
		c:ROM = {
		owns = 1
		OR = {
		rank = major_power
		rank = great_power
		}
		}
		owns = 3256
		OR = {
		rank = major_power
		rank = great_power
		}
		OR = {
		has_completed_mission = carthaginian_magna_graecia_expansion_mission
		any_neighbour_country = {
		tag = ROM
		}
		any_owned_province = {
		OR = {
		is_in_area = neapolis_area
		is_in_area = heraclea_area
		AND = {
		is_in_region = central_italy_region
		NOR = {
		is_in_area = sardinia_meridionalis
		is_in_area = sardinia_septentrionalis
		is_in_area = corsica_area
		}
		}
		is_in_region = cisalpine_gaul_region
		}
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_23 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = carthaginian_mission_07_phoenicia }
		has_dlc = "The Punic Wars"
		primary_culture = carthaginian
		NOR = {
		AND = {
		exists = p:741.owner
		p:741.owner = {
		country_culture_group = west_levantine
		}
		}
		AND = {
		exists = p:749.owner
		p:749.owner = {
		country_culture_group = west_levantine
		}
		}
		AND = {
		exists = p:768.owner
		p:768.owner = {
		country_culture_group = west_levantine
		}
		}
		AND = {
		exists = p:783.owner
		p:783.owner = {
		country_culture_group = west_levantine
		}
		}
		}
		any_owned_province = {
		OR = {
		is_in_region = lower_egypt_region
		is_in_region = palestine_region
		is_in_region = syria_region
		is_in_region = cilicia_region
		is_in_region = asia_region
		is_in_area = crete_area
		is_in_area = aegean_islands_area
		}
		}
		num_of_cities > 250 # Late Expansion
		has_civil_war = no
        }
        set_variable = { name = debug_mission_24 value = yes }
    }

    else_if = {
        limit = {
            		has_variable = num_of_completed_missions
		var:num_of_completed_missions >= 2
		NOT = { has_variable = carthaginian_mission_08_naval }
		has_dlc = "The Punic Wars"
		primary_culture = carthaginian
		owns = 3256
		any_owned_province = {
		count >= 10
		num_of_port_building >= 1
		OR = {
		is_in_region = africa_region
		is_in_region = numidia_region
		is_in_region = mauretainia_region
		}
		}
		OR = {
		rank = great_power
		rank = major_power
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_25 value = yes }
    }

    else_if = {
        limit = {
            		has_variable = num_of_completed_missions
		var:num_of_completed_missions >= 2
		NOT = { has_variable = carthaginian_mission_09_iberia_2 }
		has_dlc = "The Punic Wars"
		tag = CAR
		has_variable = carthaginian_iberia_expansion_mission_task_final_2
		has_civil_war = no
        }
        set_variable = { name = debug_mission_26 value = yes }
    }

    else_if = {
        limit = {
            		has_variable = num_of_completed_missions
		var:num_of_completed_missions >= 3
		NOT = { has_variable = carthaginian_mission_10_trade }
		has_dlc = "The Punic Wars"
		primary_culture = carthaginian
		OR = {
		rank = great_power
		rank = major_power
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_27 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = diadochi_empire }
		has_dlc = "Heirs of Alexander"
		OR = {
		OR = { # Seleukids
		tag = SEL
		has_variable = sel_unique_mech
		}
		OR = {
		tag = TRE
		has_variable = me_thrace_start_flag
		}
		OR = { # Macedon
		tag = MAC
		has_variable = me_mac_start_flag
		}
		tag = EGY
		OR = { # Antigonids
		tag = PRY
		has_variable = me_ant_start_flag
		}
		tag = ALX
		tag = EPI
		has_variable = magid_kingdom_flag
		has_variable = allow_hellenistic_empire_tree_var # Cappadocia + Pentapotamia
		tag = CIA
		has_variable = caria_macedon_missions_unlocked
		}
		has_civil_war = no
		is_monarchy = yes
		OR = {
		AND = { # Macedon
		has_completed_mission = macedon_01_legacy
		has_completed_mission = macedon_02_mastery
		}
		OR = { # Antigonids
		has_completed_mission = antigonid_01_vision
		has_completed_mission = antigonid_03_birthright
		}
		AND = { # Seleukids
		has_completed_mission = seleukid_mission_02_syria
		has_completed_mission = seleukid_mission_04_palestine
		}
		AND = { # Egypt
		has_completed_mission = egypt_mission_3_empire
		has_completed_mission = egypt_mission_4_border
		}
		AND = { # Thrace
		has_completed_mission = thrace_03_philhellene
		has_completed_mission = thracian_01_hellespont
		}
		AND = { # Caria
		has_completed_mission = caria_missions
		has_completed_mission = macedon_02_mastery
		}
		AND = { # Epirus
		has_dlc = "Epirus Flavor Pack"
		has_completed_mission = epirus_3_greece
		}
		has_variable = magid_kingdom_flag
		has_variable = allow_hellenistic_empire_tree_var # Cappadocia
		}
        }
        set_variable = { name = debug_mission_28 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = epirus_mission_01_epirus }
		has_dlc = "Epirus Flavor Pack"
		OR = {
		tag = EPI
		has_variable = me_epirus_start_flag
		}
		is_monarchy = yes
		has_civil_war = no
        }
        set_variable = { name = debug_mission_29 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = epirus_mission_02_magna }
		has_dlc = "Epirus Flavor Pack"
		OR = {
		tag = EPI
		has_variable = me_epirus_start_flag
		}
		has_completed_mission = epirus_1_epirus
		region:magna_graecia_region = {
		any_region_province = {
		count >= 1
		NOT = { owned_or_subject_owned = root }
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_30 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = epirus_mission_03_greece }
		has_dlc = "Epirus Flavor Pack"
		OR = {
		tag = EPI
		has_variable = me_epirus_start_flag
		}
		has_completed_mission = epirus_1_epirus
		region:greece_region = {
		any_region_province = {
		count >= 1
		NOT = { owned_or_subject_owned = root }
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_31 value = yes }
    }

    else_if = {
        limit = {
            		tag = ETR
		NOT = { has_variable = etrurian_mission_one_cooldown }
        }
        set_variable = { name = debug_mission_32 value = yes }
    }

    else_if = {
        limit = {
            		tag = ETR
		NOT = { has_variable = mission_cooldown_var }
		OR = {
		owns_or_subject_owns = 1
		has_completed_mission = etruria_roman_incursion
		}
        }
        set_variable = { name = debug_mission_33 value = yes }
    }

    else_if = {
        limit = {
            		tag = FGD
		NOT = { has_variable = mission_cooldown_var }
        }
        set_variable = { name = debug_mission_34 value = yes }
    }

    else_if = {
        limit = {
            		not = { has_variable = germania_mission }
		not = {tag = GEE}
		has_civil_war = no
		OR = {
		has_variable = germania_mission_allowed_var
		any_owned_province = {
		count >= 50
		OR =  {
		is_in_region = germania_region
		is_in_region = germania_superior_region
		is_in_region = venedia_region
		is_in_region = vistulia_region
		is_in_region = scandinavia_region
		is_in_region = bohemia_area
		}
		}
		}
        }
        set_variable = { name = debug_mission_35 value = yes }
    }

    else_if = {
        limit = {
            		tag = GTE
		NOT = { has_variable = getae_mission_cooldown }
        }
        set_variable = { name = debug_mission_36 value = yes }
    }

    else_if = {
        limit = {
            		tag = CRD
		NOT = { has_variable = mission_cooldown_var }
        }
        set_variable = { name = debug_mission_37 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = greek_mission_1_black_sea }
		OR = {
		AND = {
		OR = {
		primary_culture = bosporan
		primary_culture = bithynian
		primary_culture = greco_pontic
		primary_culture = thracian
		}
		capital_scope = {
		OR = {
		is_in_region = bithynia_region
		is_in_region = colchis_region
		is_in_region = taurica_region
		is_in_area = olbia_area
		is_in_area = scythia_area
		is_in_area = moesia_orientalis_area
		is_in_area = haemimontus_area
		is_in_area = europa_area
		is_in_area = clepia_area
		}
		}
		OR = {
		num_of_cities = 1
		any_owned_province = {
		num_of_port_building >= 1
		OR = {
		is_in_region = bithynia_region
		is_in_region = colchis_region
		is_in_region = taurica_region
		is_in_area = olbia_area
		is_in_area = scythia_area
		is_in_area = moesia_orientalis_area
		is_in_area = haemimontus_area
		is_in_area = europa_area
		is_in_area = clepia_area
		}
		}
		}
		}
		AND = {
		country_culture_group = hellenic
		OR = {
		tag = MGA
		tag = MLO
		num_of_cities >= 25
		}
		any_owned_province = {
		is_coastal = yes
		OR = {
		is_in_region = asia_region
		is_in_region = bithynia_region
		is_in_region = thrace_region
		}
		}
		}
		}
		NOR = {
		tag = TRE
		tag = MAC
		tag = PRY
		tag = SEL
		tag = EGY
		tag = RHO
		tag = THE
		tag = DL2
		tag = BIT
		tag = ALX
		tag = BPK
		tag = CIA
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_38 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = greek_mission_2_greece }
		trigger_if = {
		limit = { has_dlc = "Magna Graecia" }
		NOR = {
		tag = SPA
		tag = ATH
		}
		}
		trigger_if = {
		limit = { tag = ARG }
		has_completed_mission = argos_missions_01_greece
		}
		trigger_if = {
		limit = { primary_culture = cretan }
		has_completed_mission = greek_6_crete
		}
		country_culture_group = hellenic
		OR = {
		capital_scope = { gre_2_greece_trigger = yes }
		AND = {
		num_of_cities >= 25
		any_owned_province = {
		is_coastal = yes
		gre_2_greece_trigger = yes
		NOT = { is_in_area = crete_area }
		}
		}
		}
		NOT = {
		region:greece_region = {
		any_region_province = {
		count = all
		NOR = {
		is_in_area = epirus_area
		is_in_area = crete_area
		}
		}
		}
		}
		NOR = {
		tag = TRE
		tag = MAC
		tag = PRY
		tag = SEL
		tag = EGY
		tag = HLA
		tag = DLA
		tag = PLL
		tag = EPI
		tag = CYR
		tag = THE
		tag = DL2
		tag = ALX
		tag = CIA
		has_civil_war = yes
		has_variable = me_ant_start_flag
		has_variable = me_mac_start_flag
		has_variable = me_thrace_start_flag
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_39 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = greek_mission_3_italiote }
		AND = {
		primary_culture = italiotian
		capital_scope = {
		OR = {
		is_in_region = magna_graecia_region
		is_in_region = central_italy_region
		}
		}
		}
		trigger_if = {
		limit = { has_dlc = "Magna Graecia" }
		NOT = { tag = SYR }
		}
		NOR = {
		tag = TRE
		tag = MAC
		tag = PRY
		tag = SEL
		tag = EGY
		tag = SYR
		tag = SII
		}
		NOT = {
		any_country = {
		tag = MGG
		has_land = yes
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_40 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = greek_mission_4_west }
		OR = {
		AND = {
		primary_culture = massalian
		capital_scope = {
		OR = {
		is_in_region = transalpine_gaul_region
		is_in_region = tarraconensis_region
		is_in_region = contestania_region
		is_in_region = baetica_region
		}
		}
		any_owned_province = {
		num_of_port_building >= 1
		OR = {
		is_in_region = transalpine_gaul_region
		is_in_region = tarraconensis_region
		is_in_region = contestania_region
		is_in_region = baetica_region
		}
		}
		}
		AND = {
		country_culture_group = hellenic
		tag = ELE
		num_of_cities >= 5
		any_owned_province = {
		num_of_port_building >= 1
		OR = {
		is_in_region = central_italy_region
		is_in_region = magna_graecia_region
		}
		}
		}
		}
		NOR = {
		tag = TRE
		tag = MAC
		tag = PRY
		tag = SEL
		tag = EGY
		}
		has_civil_war = no
		NOT = {
		any_country = {
		tag = PHK
		has_land = yes
		}
		}
        }
        set_variable = { name = debug_mission_41 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = greek_mission_5_pontus }
		is_republic = yes
		OR = {
		tag = APL
		tag = MES
		tag = ODS
		tag = KTS
		tag = ISO
		AND = {
		OR = {
		primary_culture = bosporan
		primary_culture = greco_pontic
		primary_culture = thracian
		}
		NOT = {
		tag = BYZ
		}
		capital_scope = {
		OR = {
		is_in_region = bithynia_region
		is_in_region = colchis_region
		is_in_region = taurica_region
		is_in_area = olbia_area
		is_in_area = scythia_area
		is_in_area = moesia_orientalis_area
		is_in_area = haemimontus_area
		is_in_area = paphlagonia_inferior_area
		is_in_area = heracleotis_area
		is_in_area = machelonia_area
		is_in_area = europa_area
		is_in_area = clepia_area
		}
		}
		OR = {
		any_owned_province = {
		num_of_port_building >= 1
		OR = {
		is_in_region = bithynia_region
		is_in_region = colchis_region
		is_in_region = taurica_region
		is_in_area = olbia_area
		is_in_area = scythia_area
		is_in_area = moesia_orientalis_area
		is_in_area = haemimontus_area
		is_in_area = europa_area
		is_in_area = paphlagonia_inferior_area
		is_in_area = heracleotis_area
		is_in_area = machelonia_area
		is_in_area = clepia_area
		}
		}
		num_of_cities = 1
		}
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_42 value = yes }
    }

    else_if = {
        limit = {
            		primary_culture = cretan
		NOT = { has_variable = crete_mission_cooldown }
		NOT = { tag = KRE }
        }
        set_variable = { name = debug_mission_43 value = yes }
    }

    else_if = {
        limit = {
            		tag = KRK
		NOT = { has_variable = mission_cooldown_var }
        }
        set_variable = { name = debug_mission_44 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		primary_culture = puntic
        }
        set_variable = { name = debug_mission_45 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = indo_parthia_mission }
		OR = {
		tag = INP
		has_variable = allowed_indo_parthia_mission
		}
		has_civil_war = no
		war = no
        }
        set_variable = { name = debug_mission_46 value = yes }
    }

    else_if = {
        limit = {
            		OR = { # these are the nations that are in anatolia that can form the pan-ionian league starting  as antigonid vassals
		tag = MLO # Miletos
		tag = ION # Ionia
		tag = IOA # Aeolia
		tag = TRD # Troas
		tag = HKS # Halikarnassos
		tag = KNI # Knidos
		tag = PYK # Physkos
		tag = KAO # Kaunos
		}
		NOT = { has_variable = mission_cooldown_var }
        }
        set_variable = { name = debug_mission_47 value = yes }
    }

    else_if = {
        limit = {
            		OR = {
		has_completed_mission = ionian_resurgence
		tag = IOU
		#has_variable = can_play_ionian_resurgence_2
		}
		NOT = {
		has_variable = ion2_mission_cooldown_var
		}
		NOT = {
		has_variable = disallow_paniona_var
		}
        }
        set_variable = { name = debug_mission_48 value = yes }
    }

    else_if = {
        limit = {
            		always = yes
		tag = LGA
		NOT = { has_variable = mission_cooldown_var }
        }
        set_variable = { name = debug_mission_49 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = macedon_mission_01_legacy }
		has_dlc = "Heirs of Alexander"
		OR = {
		tag = MAC
		has_variable = me_mac_start_flag
		}
		is_monarchy = yes
		current_date < 453.11.1
		has_civil_war = no
        }
        set_variable = { name = debug_mission_50 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = macedon_mission_02_mastery }
		has_dlc = "Heirs of Alexander"
		OR = {
		AND = {
		OR = {
		tag = MAC
		has_variable = me_mac_start_flag
		}
		has_completed_mission = macedon_01_legacy
		}
		AND = {
		has_variable = me_ant_start_flag
		has_global_variable = antigonus_the_failure
		has_completed_mission = antigonid_02_aspiration
		}
		has_variable = caria_macedon_missions_unlocked
		}
		is_monarchy = yes
		has_civil_war = no
        }
        set_variable = { name = debug_mission_51 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = macedon_mission_03_adriatic }
		has_dlc = "Heirs of Alexander"
		OR = {
		AND = {
		OR = {
		tag = MAC
		has_variable = me_mac_start_flag
		}
		has_completed_mission = macedon_01_legacy
		}
		AND = {
		has_variable = me_ant_start_flag
		has_global_variable = antigonus_the_failure
		has_completed_mission = antigonid_02_aspiration
		}
		has_variable = caria_macedon_missions_unlocked
		has_variable = epi_macedon_missions_unlocked
		}
		is_monarchy = yes
		has_civil_war = no
        }
        set_variable = { name = debug_mission_52 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = macedon_04_lighthouse }
		has_dlc = "Heirs of Alexander"
		calc_true_if = {
		amount >= 2
		has_completed_mission = macedon_01_legacy
		has_completed_mission = antigonid_02_aspiration
		has_completed_mission = macedon_03_adriatic
		has_completed_mission = macedon_02_mastery
		has_completed_mission = macedon_05_rhodopes
		}
		OR = {
		AND = {
		OR = {
		tag = MAC
		has_variable = me_mac_start_flag
		}
		has_completed_mission = macedon_01_legacy
		}
		AND = {
		has_variable = me_ant_start_flag
		has_global_variable = antigonus_the_failure
		has_completed_mission = antigonid_02_aspiration
		}
		has_variable = caria_macedon_missions_unlocked
		has_variable = epi_macedon_missions_unlocked
		}
		is_monarchy = yes
		has_civil_war = no
		owns = 379
        }
        set_variable = { name = debug_mission_53 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = macedon_05_rhodopes }
		has_dlc = "Heirs of Alexander"
		OR = {
		AND = {
		OR = {
		tag = MAC
		has_variable = me_mac_start_flag
		}
		has_completed_mission = macedon_01_legacy
		}
		AND = {
		has_variable = me_ant_start_flag
		has_global_variable = antigonus_the_failure
		has_completed_mission = antigonid_02_aspiration
		}
		has_variable = caria_macedon_missions_unlocked
		has_variable = epi_macedon_missions_unlocked
		}
		is_monarchy = yes
		has_civil_war = no
        }
        set_variable = { name = debug_mission_54 value = yes }
    }

    else_if = {
        limit = {
            		tag = MLO
		has_completed_mission = ionian_resurgence # enable for release
		NOT = {
		has_variable = miletos_mission_cooldown
		}
        }
        set_variable = { name = debug_mission_55 value = yes }
    }

    else_if = {
        limit = {
            		always = no
		tag = PON
		NOT = { has_variable = mission_cooldown_var }
        }
        set_variable = { name = debug_mission_56 value = yes }
    }

    else_if = {
        limit = {
            		tag = PAP
		NOT = { has_variable = mission_cooldown_var }
        }
        set_variable = { name = debug_mission_57 value = yes }
    }

    else_if = {
        limit = {
            		NOR = {
		has_variable = mission_cooldown_var
		has_variable = patavi_patavium_spawned_tag
		is_ai = yes
		}
		OR = {
		has_variable = spartan_patavium_spawned_tag
		tag = PAT
		}
        }
        set_variable = { name = debug_mission_58 value = yes }
    }

    else_if = {
        limit = {
            		NOR = {
		has_variable = mission_cooldown_var
		has_variable = patavi_patavium_spawned_tag
		}
		OR = {
		has_variable = spartan_patavium_spawned_tag
		tag = PAT
		}
		has_completed_mission = patauion_mission_01
        }
        set_variable = { name = debug_mission_59 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = rhaetia_alpine_mission }
		has_civil_war = no
		OR = {
		primary_culture = rhaetian
		tag = RAE
		}
		any_owned_province = {
		count >= 1
		is_in_area = alpes_raetiae_occidentalis
		}
        }
        set_variable = { name = debug_mission_60 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = roman_mission_01_italia }
		has_dlc = "The Punic Wars"
		tag = ROM
		region:central_italy_region = { # Roma.Italia
		any_region_province = {
		NOR = {
		is_in_area = corsica_area
		is_in_area = sardinia_septentrionalis
		is_in_area = sardinia_meridionalis
		owned_or_subject_owned = root
		}
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_61 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = roman_mission_02_provincia }
		has_dlc = "The Punic Wars"
		tag = ROM
		region:central_italy_region = {
		any_region_province = {
		count >= 50
		owned_or_subject_owned = root
		}
		}
		OR = {
		NOT = { owns = 3515 }
		NOT = { owns = 3481 }
		NOT = { owns = 3504 }
		NOT = { owns = 94 }
		NOT = { owns = 80 }
		NOT = { owns = 84 }
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_62 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = roman_mission_03_cisalpine_gaul }
		has_dlc = "The Punic Wars"
		tag = ROM
		owns_or_subject_owns = 142
		region:cisalpine_gaul_region = { # Mutina.Cisalpine Gaul
		any_region_province = {
		NOT = { owned_or_subject_owned = root }
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_63 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = roman_mission_04_punic_rivals }
		has_dlc = "The Punic Wars"
		tag = ROM
		c:CAR = {
		has_land = yes
		num_of_cities > 50
		NOR = {
		alliance_with = c:ROM
		is_subject_of = c:ROM
		}
		}
		AND = {
		OR = {
		owns_or_subject_owns_region = central_italy_region
		has_completed_mission = roman_1_italia
		}
		OR = {
		owns_or_subject_owns_region = magna_graecia_region
		has_completed_mission = roman_2_provincia
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_64 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = roman_mission_05_hispanian_ambition }
		tag = ROM
		has_dlc = "The Punic Wars"
		owns = 3515
		OR = {
		has_completed_mission = roman_4_punic_rivals
		OR = {
		region:baetica_region = { #Carteia/Baetica
		any_region_province = { owned_or_subject_owned = c:ROM }
		}
		region:contestania_region = { #Mastia/Contestania
		any_region_province = {
		NOT = { is_in_area = balearides_area }
		owned_or_subject_owned = c:ROM
		}
		}
		region:tarraconensis_region = { #Emporion/Tarraconensis
		any_region_province = { owned_or_subject_owned = c:ROM }
		}
		region:lusitania_region = { #Olisipo/Lusitania
		any_region_province = { owned_or_subject_owned = c:ROM }
		}
		region:gallaecia_region = { #Lancia/Gallaecia
		any_region_province = { owned_or_subject_owned = c:ROM }
		}
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_65 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = roman_mission_06_africa }
		has_dlc = "The Punic Wars"
		tag = ROM
		OR = {
		has_completed_mission = roman_4_punic_rivals
		OR = {
		area:tripolitania_occidentalis_area = {
		any_area_province = { owned_or_subject_owned = c:ROM }
		}
		area:tripolitania_centralis_area = {
		any_area_province = { owned_or_subject_owned = c:ROM }
		}
		region:mauretainia_region = {
		any_region_province = { owned_or_subject_owned = c:ROM }
		}
		region:numidia_region = {
		any_region_province = { owned_or_subject_owned = c:ROM }
		}
		region:africa_region = {
		any_region_province = { owned_or_subject_owned = c:ROM }
		}
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_66 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = roman_mission_07_invading_gaul }
		has_dlc = "The Punic Wars"
		tag = ROM
		owns = 3549
		OR = {
		has_completed_mission = roman_3_cisalpine_gaul
		owns_or_subject_owns_region = cisalpine_gaul_region
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_67 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = roman_mission_08_greece }
		has_dlc = "The Punic Wars"
		tag = ROM
		OR = {
		any_owned_province = {
		is_in_region = greece_region
		}
		has_completed_mission = roman_1_italia
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_68 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = roman_mission_09_eastern_glory }
		has_dlc = "The Punic Wars"
		tag = ROM
		OR = {
		has_completed_mission = roman_mission_08_greece
		any_owned_province = {
		OR = {
		is_in_region = thrace_region
		is_in_region = asia_region
		is_in_region = bithynia_region
		}
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_69 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = roman_mission_010_invading_britannia }
		has_dlc = "The Punic Wars"
		tag = ROM
		owns = 2440
		OR = {
		has_completed_mission = roman_7_invading_gaul
		owns_or_subject_owns_region = central_gaul_region
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_70 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = roman_mission_011_rhaetia_vindelicia }
		has_dlc = "The Punic Wars"
		tag = ROM
		owns = 2440
		OR = {
		has_completed_mission = roman_7_invading_gaul
		owns_or_subject_owns_region = cisalpine_gaul_region
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_71 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = roman_mission_012_illyria_pannonia }
		has_dlc = "The Punic Wars"
		tag = ROM
		owns = 415
		OR = {
		has_completed_mission = roman_mission_08_greece
		owns_or_subject_owns_region = greece_region
		owns_or_subject_owns_region = macedonia_region
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_72 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		primary_culture = salluvian
        }
        set_variable = { name = debug_mission_73 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = seleukid_mission_01_babylonia }
		has_dlc = "Heirs of Alexander"
		OR = {
		tag = SEL
		has_variable = sel_unique_mech
		}
		has_civil_war = no
		is_monarchy = yes
        }
        set_variable = { name = debug_mission_74 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = seleukid_mission_02_syria }
		has_dlc = "Heirs of Alexander"
		OR = {
		tag = SEL
		has_variable = sel_unique_mech
		}
		has_civil_war = no
		is_monarchy = yes
        }
        set_variable = { name = debug_mission_75 value = yes }
    }

    else_if = {
        limit = {
            		OR = {
		has_completed_mission = seleukid_mission_02_syria
		owns_or_subject_owns_area = cilicia_pedias_area
		}
		NOT = { has_variable = seleukid_mission_03_anatolia }
		has_dlc = "Heirs of Alexander"
		OR = {
		tag = SEL
		has_variable = sel_unique_mech
		}
		has_civil_war = no
		is_monarchy = yes
        }
        set_variable = { name = debug_mission_76 value = yes }
    }

    else_if = {
        limit = {
            		OR = {
		has_completed_mission = seleukid_mission_02_syria
		owns_or_subject_owns_region = syria_region
		}
		NOT = { has_variable = seleukid_mission_04_palestine }
		has_dlc = "Heirs of Alexander"
		OR = {
		tag = SEL
		has_variable = sel_unique_mech
		}
		has_civil_war = no
		is_monarchy = yes
        }
        set_variable = { name = debug_mission_77 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		is_subject = yes #Set for now since the mission is designed around being one
		has_civil_war = no
		tag = SOO
        }
        set_variable = { name = debug_mission_78 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = spartan_mission_01_laconia }
		has_dlc = "Magna Graecia"
		OR = {
		tag = SPA
		has_variable = spa_unique_mech
		}
		NOR = {
		has_completed_mission = 01_sparta_missions_02_peloponnesus
		has_completed_mission = spartan_mission_03_peloponnesian_league
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_79 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = spartan_mission_02_peloponnesus }
		OR = {
		tag = SPA
		has_variable = spa_unique_mech
		}
		has_dlc = "Magna Graecia"
		has_completed_mission = spartan_mission_01_laconia
		NOT = { has_completed_mission = spartan_mission_03_peloponnesian_league }
		has_civil_war = no
        }
        set_variable = { name = debug_mission_80 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = spartan_mission_03_peloponnesian_league }
		has_dlc = "Magna Graecia"
		OR = {
		tag = SPA
		has_variable = spa_unique_mech
		}
		OR = {
		has_completed_mission = 01_sparta_missions_02_peloponnesus
		AND = {
		area:south_peleponnesos_area = {
		any_area_province = {
		count = all
		OR = {
		owned_or_subject_owned = root
		province_id = 460
		province_id = 442
		}
		}
		}
		area:arcadia_area = {
		any_area_province = {
		count = all
		owned_or_subject_owned = root
		}
		}
		area:achaea_area = {
		any_area_province = {
		count = all
		owned_or_subject_owned = root
		}
		}
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_81 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = spartan_mission_04_rivals }
		has_dlc = "Magna Graecia"
		OR = {
		tag = SPA
		has_variable = spa_unique_mech
		}
		OR = {
		has_completed_mission = 01_sparta_missions_02_peloponnesus
		has_completed_mission = spartan_mission_03_peloponnesian_league
		}
		calc_true_if = {
		amount >= 2
		AND = {
		c:ATH = {
		num_of_cities >= 1
		}
		NOT = { owns = 416 }
		}
		NOT = { owns = 426 }
		AND = {
		c:MAC = {
		num_of_cities >= 1
		}
		NOR = {
		owns = 379
		owns = 373
		}
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_82 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = syracuse_mission_01_democracy }
		has_dlc = "Magna Graecia"
		OR = {
		tag = SYR
		has_variable = me_syracuse_start_flag
		}
		is_monarchy = yes
		country_culture_group = hellenic
		current_date > 460.1.1
		custom_tooltip = {
		text = democracy_eligible_tooltip
		current_ruler = {
		NOR = {
		age > 65
		char_is_ill_trigger = yes
		}
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_83 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = syracuse_mission_02_sicily }
		has_dlc = "Magna Graecia"
		OR = {
		tag = SYR
		has_variable = me_syracuse_start_flag
		}
		is_monarchy = yes
		country_culture_group = hellenic
		NOR = {
		tag = SII
		AND = {
		area:east_sicily_area = {
		any_area_province = {
		count = all
		owner = root
		}
		}
		area:syracuse_area = {
		any_area_province = {
		count = all
		owner = root
		}
		}
		area:west_sicily_area = {
		any_area_province = {
		count = all
		owner = root
		}
		}
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_84 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = syracuse_mission_03_sicily }
		has_dlc = "Magna Graecia"
		OR = {
		tag = SYR
		tag = SII
		has_variable = me_syracuse_start_flag
		}
		c:CAR = {
		has_land = yes
		NOT = { alliance_with = root }
		}
		country_culture_group = hellenic
		num_of_cities > 5
		OR = {
		num_of_cities > 14
		has_completed_mission = me_syracuse_2_sicily
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_85 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = syracuse_mission_04_magna }
		has_dlc = "Magna Graecia"
		OR = {
		tag = SYR
		tag = SII
		has_variable = me_syracuse_start_flag
		}
		country_culture_group = hellenic
		OR = {
		has_completed_mission = me_syracuse_2_sicily
		has_completed_mission = me_syracuse_3_punic
		}
		NOT = {
		region:magna_graecia_region = {
		any_region_province = {
		NOR = {
		is_in_area = syracuse_area
		is_in_area = east_sicily_area
		is_in_area = west_sicily_area
		}
		count >= 10
		owner = root
		}
		}
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_86 value = yes }
    }

    else_if = {
        limit = {
            		tag = TAY
        }
        set_variable = { name = debug_mission_87 value = yes }
    }

    else_if = {
        limit = {
            		has_completed_mission = tayuan_mission_1
		NOT = {
		has_variable = tay2_mission_cooldown_var
		}
        }
        set_variable = { name = debug_mission_88 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = thrace_mission_03_philhellene }
		has_dlc = "Heirs of Alexander"
		OR = {
		tag = TRE
		has_variable = me_thrace_start_flag
		}
		is_monarchy = yes
        }
        set_variable = { name = debug_mission_89 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = thracian_mission_02_paper_kingdom }
		has_dlc = "Heirs of Alexander"
		OR = {
		tag = TRE
		has_variable = me_thrace_start_flag
		}
		is_monarchy = yes
        }
        set_variable = { name = debug_mission_90 value = yes }
    }

    else_if = {
        limit = {
            		has_dlc = "Heirs of Alexander"
		is_monarchy = yes
		tag = TRE
		exists = c:TRE.current_ruler
		exists = fam:Alkimachid
		c:TRE.current_ruler.family = fam:Alkimachid
		primary_culture = macedonian
		NOT = { has_variable = thracian_mission_01_hellespont }
        }
        set_variable = { name = debug_mission_91 value = yes }
    }

    else_if = {
        limit = {
            		primary_culture = turdetanian
		NOT = { has_variable = turdetanian_mission_01_sons_baetica_cooldown }
		NOT = { tag = TAT }
        }
        set_variable = { name = debug_mission_92 value = yes }
    }

    else_if = {
        limit = {
            		primary_culture = turdetanian
		has_completed_mission = turdetanian_mission_01_sons_baetica_inv
		NOT = { has_variable = turdetanian_mission_02_heirs_tartessia_cooldown }
        }
        set_variable = { name = debug_mission_93 value = yes }
    }

    else_if = {
        limit = {
            		primary_culture = turdetanian
		has_completed_mission = turdetanian_mission_02_heirs_tartessia_inv
		NOT = { has_variable = turdetanian_mission_03_conquerors_cooldown }
        }
        set_variable = { name = debug_mission_94 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		has_civil_war = no
		OR = {
		primary_culture = umbrian
		tag = UMB
		}
        }
        set_variable = { name = debug_mission_95 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		country_culture_group = illyrian_group
        }
        set_variable = { name = debug_mission_96 value = yes }
    }

    else_if = {
        limit = {
            		primary_culture = aksumite
		NOT = { has_variable = aksum_mission_cooldown }
        }
        set_variable = { name = debug_mission_97 value = yes }
    }

    else_if = {
        limit = {
            		primary_culture = aksumite
		has_completed_mission = aksum_mission_1_ethiopia
		NOT = { has_variable = aksum_mission_cooldown_2 }
        }
        set_variable = { name = debug_mission_98 value = yes }
    }

    else_if = {
        limit = {
            		tag = OLS
        }
        set_variable = { name = debug_mission_99 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = barbaricum_aremorica_mission }
		has_civil_war = no
		primary_culture = aremorican
		is_tribal = yes
        }
        set_variable = { name = debug_mission_100 value = yes }
    }

    else_if = {
        limit = {
            		OR = {
		tag = ARG
		heritage = argive_heritage
		}
		NOT = { has_variable = argos_missions_cooldown }
        }
        set_variable = { name = debug_mission_101 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		tag = IBR
		always = no
        }
        set_variable = { name = debug_mission_102 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = arseka_cooldown_var }
		OR = {
		tag = SGS
		primary_culture = edetanian
		}
        }
        set_variable = { name = debug_mission_103 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		tag = ARY
        }
        set_variable = { name = debug_mission_104 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = atropatene_mission_cooldown }
		OR = {
		tag = MAP # TRIGGER FIELD - Sets the conditions for the mission to appear in your list
		#  AND = {
		#     tag = MIT
		#     religion = mithra_religion #may disable entirely
		# }
		}
        }
        set_variable = { name = debug_mission_105 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = bactrian_mission_01_bactria }
		tag = BAC
		has_civil_war = no
		is_monarchy = yes
		primary_culture = macedonian
		owns_area = bactria_area
        }
        set_variable = { name = debug_mission_106 value = yes }
    }

    else_if = {
        limit = {
            		has_completed_mission = bactrian_mission_01_bactria
		NOT = {
		has_variable = bactrian_mission_02_parthia
		}
		tag = BAC
		has_civil_war = no
		is_monarchy = yes
		primary_culture = macedonian
		owns_area = bactria_area
        }
        set_variable = { name = debug_mission_107 value = yes }
    }

    else_if = {
        limit = {
            		has_completed_mission = bactrian_mission_02_parthia
		NOT = {
		has_variable = bactrian_mission_03_aria
		}
		tag = BAC
		has_civil_war = no
		is_monarchy = yes
		primary_culture = macedonian
		owns_area = bactria_area
        }
        set_variable = { name = debug_mission_108 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = barbaricum_bastarnia_mission }
		has_civil_war = no
		primary_culture = bastarnae
		is_tribal = yes
        }
        set_variable = { name = debug_mission_109 value = yes }
    }

    else_if = {
        limit = {
            		primary_culture = blemmyan
		NOT = { has_variable = mission_cooldown_var }
        }
        set_variable = { name = debug_mission_110 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = barbaricum_bohemia_mission }
		has_civil_war = no
		tag = BOA
        }
        set_variable = { name = debug_mission_111 value = yes }
    }

    else_if = {
        limit = {
            		tag = BIT
		NOT = { has_variable = bithynia_mission_cooldown }
        }
        set_variable = { name = debug_mission_112 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		primary_culture = caledonian
        }
        set_variable = { name = debug_mission_113 value = yes }
    }

    else_if = {
        limit = {
            		OR = {
		tag = CAP
		trigger_if = {
		limit = { exists = global_var:ARI_tag }
		this = global_var:ARI_tag
		}
		}
		war = no #Stop AI from taking these missions while the player rebellion is ongoing
		NOT = {
		has_variable = cap_mis_abort_var
		}
        }
        set_variable = { name = debug_mission_114 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = caria_missions_cooldown_var }
		tag = CIA
        }
        set_variable = { name = debug_mission_115 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		has_civil_war = no
		tag = COL
        }
        set_variable = { name = debug_mission_116 value = yes }
    }

    else_if = {
        limit = {
            		OR = { # if commented out, it was for testing reasons
		# placeholder until they get missions
		tag = HDM # Hadhramut
		tag = MIN # Ma'in
		has_completed_mission = saba_mission
		has_completed_mission = qataba_mission
		tag = HMJ # Himyar - gets created in this mission, no anterior mission
		has_variable = is_himyar_var
		tag = AX6 # Aad
		tag = YMN # Yamnat - Yemen formable
		}
		NOT = {
		has_variable = south_arabia_mission_cooldown
		}
        }
        set_variable = { name = debug_mission_117 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = cyrene_1_mission_academies }
		OR = {
		tag = CYR
		has_variable = cyr_egy_var
		}
		has_civil_war = no
		has_completed_mission = cyrene_1_magas
        }
        set_variable = { name = debug_mission_118 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = cyrene_1_mission_intoegypt }
		tag = CYR
		is_monarchy = yes
		has_civil_war = no
		exists = c:EGY
		has_completed_mission = cyrene_1_magas
        }
        set_variable = { name = debug_mission_119 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = cyrene_1_mission_magas }
		tag = CYR
		has_civil_war = no
		war = no
        }
        set_variable = { name = debug_mission_120 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = cyrene_1_mission_ophellas }
		tag = CYR
		has_civil_war = no
		has_completed_mission = cyrene_1_magas
        }
        set_variable = { name = debug_mission_121 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = cyrene_1_mission_reform }
		tag = CYR
		is_monarchy = yes
		has_civil_war = no
		has_completed_mission = cyrene_1_magas
		var:num_of_completed_missions >= 3
        }
        set_variable = { name = debug_mission_122 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = cyrene_1_mission_thera }
		tag = CYR
		has_civil_war = no
		has_completed_mission = cyrene_1_magas
        }
        set_variable = { name = debug_mission_123 value = yes }
    }

    else_if = {
        limit = {
            		tag = DUM
		NOT = { has_variable = dumnonia_mission_cooldown }
        }
        set_variable = { name = debug_mission_124 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		has_civil_war = no
		tag = EBS
        }
        set_variable = { name = debug_mission_125 value = yes }
    }

    else_if = {
        limit = {
            		has_dlc = "Heirs of Alexander"
		NOT = { has_variable = egypt_mission_1_sea }
		OR = {
		tag = EGY
		has_variable = cyr_egy_var
		}
		is_monarchy = yes
		owns = 516
		primary_culture = macedonian
		OR = {
		AND = {
		exists = fam:Lagid
		c:EGY.current_ruler.family = fam:Lagid
		}
		current_ruler = { has_trait = argeads }
		current_ruler = { has_trait = lagids }
		has_variable = cyr_egy_var
		}
		OR = {
		has_completed_mission = egypt_mission_3_empire
		has_completed_mission = egypt_mission_4_border
		has_variable = cyr_egy_var
		}
        }
        set_variable = { name = debug_mission_126 value = yes }
    }

    else_if = {
        limit = {
            		has_dlc = "Heirs of Alexander"
		NOT = { has_variable = egypt_mission_2_pharaoh }
		OR = {
		tag = EGY
		has_variable = cyr_egy_var
		}
		is_monarchy = yes
		owns_or_subject_owns_area = western_delta_area
		owns_or_subject_owns_area = heptanomis_area
		primary_culture = macedonian
		OR = {
		AND = {
		exists = fam:Lagid
		c:EGY.current_ruler.family = fam:Lagid
		}
		current_ruler = { has_trait = argeads }
		current_ruler = { has_trait = lagids }
		has_variable = cyr_egy_var
		}
        }
        set_variable = { name = debug_mission_127 value = yes }
    }

    else_if = {
        limit = {
            		has_dlc = "Heirs of Alexander"
		NOT = { has_variable = egypt_mission_3_empire }
		OR = {
		tag = EGY
		has_variable = cyr_egy_var
		}
		is_monarchy = yes
		primary_culture = macedonian
		OR = {
		AND = {
		exists = fam:Lagid
		c:EGY.current_ruler.family = fam:Lagid
		}
		current_ruler = { has_trait = argeads }
		current_ruler = { has_trait = lagids }
		has_variable = cyr_egy_var
		}
		trigger_if = {
		limit = {
		c:CYR = {
		is_ai = no
		}
		}
		current_date >= 453.6.6
		}
        }
        set_variable = { name = debug_mission_128 value = yes }
    }

    else_if = {
        limit = {
            		has_dlc = "Heirs of Alexander"
		NOT = { has_variable = egypt_mission_4_border }
		OR = {
		tag = EGY
		has_variable = cyr_egy_var
		}
		is_monarchy = yes
		primary_culture = macedonian
		owns = 516
		#        exists = c:EGY.current_ruler
		OR = {
		AND = {
		exists = fam:Lagid
		c:EGY.current_ruler.family = fam:Lagid
		}
		current_ruler = { has_trait = argeads }
		current_ruler = { has_trait = lagids }
		has_variable = cyr_egy_var
		}
        }
        set_variable = { name = debug_mission_129 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = fezzani_mission_cooldown }
		OR = {
		tag = MKA # MAKAE
		tag = PSY # PSYLLI
		tag = NAS # NASAMONES
		tag = GAR # GARAMANTES
		tag = MHY # MACHYLES
		tag = PHZ # PHAZANI
		} # TRIGGER FIELD - Sets the conditions for the mission to appear in your list
        }
        set_variable = { name = debug_mission_130 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = barbaricum_francia_austria_mission }
		has_civil_war = no
		primary_culture = istvaeonic
		tag = FRA
        }
        set_variable = { name = debug_mission_131 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = barbaricum_form_francia_mission }
		has_civil_war = no
		primary_culture = istvaeonic
		is_tribal = yes
        }
        set_variable = { name = debug_mission_132 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = barbaricum_gallia_cisalpina_mission }
		has_civil_war = no
		primary_culture = lepontic
		is_tribal = yes
        }
        set_variable = { name = debug_mission_133 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = barbaricum_gothonia_mission }
		has_civil_war = no
		primary_culture = gothonic
        }
        set_variable = { name = debug_mission_134 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = gymnaesian_mission_cooldown }
		primary_culture = talaiotic # TRIGGER FIELD - Sets the conditions for the mission to appear in your list
        }
        set_variable = { name = debug_mission_135 value = yes }
    }

    else_if = {
        limit = {
            		NOT = {
		has_variable = hcp_mission_01
		}
		tag = HCP
		has_civil_war = no
		is_monarchy = yes
		NOT = {
		has_country_modifier = dhe_heraclea_pontica_greek_modifier
		}
		primary_culture = pasargadi
        }
        set_variable = { name = debug_mission_136 value = yes }
    }

    else_if = {
        limit = {
            		NOT = {
		has_variable = hcp_mission_02
		}
		tag = HCP
		has_completed_mission = hcp_mission_01
		has_civil_war = no
		is_monarchy = yes
		NOT = {
		has_country_modifier = dhe_heraclea_pontica_greek_modifier
		}
		primary_culture = pasargadi
        }
        set_variable = { name = debug_mission_137 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		has_civil_war = no
		OR = {
		primary_culture = voluntian
		primary_culture = ivernian
		}
        }
        set_variable = { name = debug_mission_138 value = yes }
    }

    else_if = {
        limit = {
            		has_completed_mission = 01_horn_of_africa
		NOT = { has_variable = african_mission_cooldown }
        }
        set_variable = { name = debug_mission_139 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		tag = IBR
        }
        set_variable = { name = debug_mission_140 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		tag = ICE
        }
        set_variable = { name = debug_mission_141 value = yes }
    }

    else_if = {
        limit = {
            		NOT = {
		has_variable = indian_mission_01_gandara
		}
		has_variable = formed_indo_greek_kingdom
		OR = {
		tag = BAC
		tag = IDC
		tag = SEL
		tag = PAR
		tag = ARS
		}
		country_culture_group = hellenic
		is_monarchy = yes
		has_civil_war = no
        }
        set_variable = { name = debug_mission_142 value = yes }
    }

    else_if = {
        limit = {
            		has_completed_mission = indian_mission_01_gandara
		NOT = { has_variable = indian_mission_02_ganges }
		has_variable = formed_indo_greek_kingdom
		OR = {
		tag = BAC
		tag = IDC
		tag = SEL
		tag = PAR
		tag = ARS
		}
		country_culture_group = hellenic
		is_monarchy = yes
		has_civil_war = no
        }
        set_variable = { name = debug_mission_143 value = yes }
    }

    else_if = {
        limit = {
            		has_completed_mission = indian_mission_02_ganges
		NOT = { has_variable = indian_mission_03_aparanta }
		has_variable = formed_indo_greek_kingdom
		OR = {
		tag = BAC
		tag = IDC
		tag = SEL
		tag = PAR
		tag = ARS
		}
		country_culture_group = hellenic
		is_monarchy = yes
		has_civil_war = no
        }
        set_variable = { name = debug_mission_144 value = yes }
    }

    else_if = {
        limit = {
            		has_completed_mission = indian_mission_03_aparanta
		NOT = {
		has_variable = indian_mission_04_dravidia
		}
		has_variable = formed_indo_greek_kingdom
		OR = {
		tag = BAC
		tag = IDC
		tag = SEL
		tag = PAR
		tag = ARS
		}
		country_culture_group = hellenic
		is_monarchy = yes
		has_civil_war = no
        }
        set_variable = { name = debug_mission_145 value = yes }
    }

    else_if = {
        limit = {
            		OR = {
		tag = JUD
		tag = ISR
		heritage = zealot_heritage
		heritage = judea_heritage
		}
		NOT = {
		heritage = samaria_heritage
		}
		religion = judaism
		NOT = { has_variable = judea_mission_cooldown }
        }
        set_variable = { name = debug_mission_146 value = yes }
    }

    else_if = {
        limit = {
            		OR = {
		tag = KAL
		AND = {
		root.capital_scope = {
		is_in_area = kalinga_area
		}
		primary_culture = kalingan
		}
		}
		NOT = { has_variable = kalingan_mission_cooldown }
        }
        set_variable = { name = debug_mission_147 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = kush_missions_cooldown }
		tag = KSH
        }
        set_variable = { name = debug_mission_148 value = yes }
    }

    else_if = {
        limit = {
            		tag = ANU
		NOT = { has_variable = lankan_mission_cooldown }
        }
        set_variable = { name = debug_mission_149 value = yes }
    }

    else_if = {
        limit = {
            		tag = ANU
		NOT = { has_variable = lankan_mission_two_cooldown }
		has_completed_mission = lankan_missions
        }
        set_variable = { name = debug_mission_150 value = yes }
    }

    else_if = {
        limit = {
            		is_ai = no
		tag = MRY
		global_var:acting_as_ashoka = {
		is_ruler = yes
		is_alive = yes
		}
		NOT = { has_variable = mauryan_mission_cooldown }
        }
        set_variable = { name = debug_mission_151 value = yes }
    }

    else_if = {
        limit = {
            		is_ai = no
		tag = MRY
		has_variable = reconquest_tree_start_var	#Starts after Shunga rebellion event fires.
		NOT = { has_global_variable = mauryan_collapse_prevented }
		NOT = { has_variable = mauryan_mission_two_cooldown }
        }
        set_variable = { name = debug_mission_152 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		primary_culture = messapian
        }
        set_variable = { name = debug_mission_153 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		tag = NMT
        }
        set_variable = { name = debug_mission_154 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = nabatea_mission_cooldown }
		tag = NBT # TRIGGER FIELD - Sets the conditions for the mission to appear in your list
        }
        set_variable = { name = debug_mission_155 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		tag = NBP
        }
        set_variable = { name = debug_mission_156 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = numidian_mission_cooldown }
		OR = {
		tag = MSS
		tag = MAU
		tag = NUM
		} # TRIGGER FIELD - Sets the conditions for the mission to appear in your list
        }
        set_variable = { name = debug_mission_157 value = yes }
    }

    else_if = {
        limit = {
            		primary_culture = nuragic
		NOT = { has_variable = nuragic_mission_cooldown }
        }
        set_variable = { name = debug_mission_158 value = yes }
    }

    else_if = {
        limit = {
            		OR ={
		has_completed_mission = nuragic_mission_01_inv
		has_completed_mission = gymnaesian_mission_01
		has_variable = nuragic_corsian_sea_people_var
		}
		NOT = { has_variable = mission_cooldown_var_sea_people }
        }
        set_variable = { name = debug_mission_159 value = yes }
    }

    else_if = {
        limit = {
            		tag = ODR
		has_civil_war = no
		NOT = {
		has_variable = odryssia_missions_01_freedom
		}
        }
        set_variable = { name = debug_mission_160 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = parthia_mission }
		OR = {
		tag = PTH
		tag = DHE
		}
		has_civil_war = no
        }
        set_variable = { name = debug_mission_161 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = parthia_mission_satrapy }
		tag = PAR
		has_civil_war = no
		#		war = no
        }
        set_variable = { name = debug_mission_162 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = paurava_missions_cooldown }
		tag = PRA
		NOT = { has_variable = is_morbin }
        }
        set_variable = { name = debug_mission_163 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		has_variable = is_morbin
        }
        set_variable = { name = debug_mission_164 value = yes }
    }

    else_if = {
        limit = {
            		tag = PES
		NOT = { has_variable = persis_missions_cooldown }
        }
        set_variable = { name = debug_mission_165 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = phoenicia_missions_cooldown_var }
		NAND = {#Don't have to worry about competition
		is_ai = yes
		any_country = {
		primary_culture = phoenician
		is_ai = no
		}
		}
		OR = {
		primary_culture = phoenician
		heritage = sidon_heritage
		heritage = arados_heritage
		heritage = byblos_heritage
		heritage = tyre_heritage
		}
        }
        set_variable = { name = debug_mission_166 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		OR = {
		tag = MIT
		AND = {
		tag = HPP
		current_ruler = { has_trait = mithridates }
		}
		}
		exists = c:PRY
        }
        set_variable = { name = debug_mission_167 value = yes }
    }

    else_if = {
        limit = {
            		has_completed_mission = pontus_kingdom_missions
		#has_variable = pontus_black_sea_missions
		NOT = { has_variable = mission_cooldown_var }
        }
        set_variable = { name = debug_mission_168 value = yes }
    }

    else_if = {
        limit = {
            		c:CAR = {
		NOT = {
		has_variable = "punic_mission_taken"
		}
		}
		is_subject_of = c:CAR
		primary_culture = carthaginian
		NOT = {
		has_variable = "punic_mission_cooldown"
		}
        }
        set_variable = { name = debug_mission_169 value = yes }
    }

    else_if = {
        limit = {
            		tag = QTB
		NOT = {
		has_variable = qataba_mission_cooldown
		}
        }
        set_variable = { name = debug_mission_170 value = yes }
    }

    else_if = {
        limit = {
            		tag = RHO
		NOT = { has_variable = rhodes_missions_cooldown }
        }
        set_variable = { name = debug_mission_171 value = yes }
    }

    else_if = {
        limit = {
            		tag = RHO
		NOT = { has_variable = rhodes_missions_two_cooldown }
		has_completed_mission = rhodes_01_aegean
        }
        set_variable = { name = debug_mission_172 value = yes }
    }

    else_if = {
        limit = {
            		tag = SHB
		NOT = {
		has_variable = saba_mission_cooldown
		}
        }
        set_variable = { name = debug_mission_173 value = yes }
    }

    else_if = {
        limit = {
            		OR = {
		tag = SMA
		heritage = samaria_heritage
		}
		religion = judaism
		NOT = { has_variable = samaria_mission_cooldown }
        }
        set_variable = { name = debug_mission_174 value = yes }
    }

    else_if = {
        limit = {
            		tag = SAM
		NOT = {
		has_variable = samnium_mission_cooldown
		}
        }
        set_variable = { name = debug_mission_175 value = yes }
    }

    else_if = {
        limit = {
            		NOT = {
		has_variable = samnium_mission_2_cooldown
		}
		has_completed_mission = samnium_mission
        }
        set_variable = { name = debug_mission_176 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = barbaricum_saxonia_anglia_mission }
		has_civil_war = no
		tag = XXS
		is_tribal = yes
        }
        set_variable = { name = debug_mission_177 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = barbaricum_saxonia_form_mission }
		has_civil_war = no
		OR = {
		primary_culture = ingvaeonic
		primary_culture = cimbrian
		}
		is_tribal = yes
        }
        set_variable = { name = debug_mission_178 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		has_civil_war = no
		primary_culture = scandian
        }
        set_variable = { name = debug_mission_179 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = scythia_mission_01_greek_stance }
		tag = SCY
		has_civil_war = no
        }
        set_variable = { name = debug_mission_180 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		has_civil_war = no
		tag = SCU
        }
        set_variable = { name = debug_mission_181 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		primary_culture = sogdian
        }
        set_variable = { name = debug_mission_182 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = barbaricum_suebi_boiohaemia_mission }
		has_civil_war = no
		primary_culture = irminonic
		is_tribal = yes
		OR = {
		tag = SBN
		tag = MCM
		tag = QUA
		}
        }
        set_variable = { name = debug_mission_183 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = barbaricum_suebi_gallia_mission }
		has_civil_war = no
		primary_culture = irminonic
		is_tribal = yes
		OR = {
		tag = SBN
		tag = SMN
		}
        }
        set_variable = { name = debug_mission_184 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = tamil_king_missions_cooldown }
		OR = {
		tag = CHL
		tag = PND
		tag = CHR
		}
        }
        set_variable = { name = debug_mission_185 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = tamilakam_missions_cooldown }
		tag = TAM
        }
        set_variable = { name = debug_mission_186 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		has_civil_war = no
		tag = AX0
        }
        set_variable = { name = debug_mission_187 value = yes }
    }

    else_if = {
        limit = {
            		has_completed_mission = 01_tasm_missions
		NOT = { has_variable = tasm_mission_cooldown_two }
        }
        set_variable = { name = debug_mission_188 value = yes }
    }

    else_if = {
        limit = {
            		OR = {
		heritage = theban_heritage
		tag = THE
		tag = DL2
		}
		NOT = { has_variable = thebes_mission_cooldown }
        }
        set_variable = { name = debug_mission_189 value = yes }
    }

    else_if = {
        limit = {
            		OR = {
		heritage = theban_heritage
		tag = THE
		tag = DL2
		}
		has_completed_mission = thebes_missions_01_restoration
		NOT = { has_variable = thebes_mission_cooldown_two }
        }
        set_variable = { name = debug_mission_190 value = yes }
    }

    else_if = {
        limit = {
            		OR = {
		heritage = theban_heritage
		tag = THE
		tag = DL2
		}
		has_completed_mission = thebes_missions_02_rivals
		NOT = { has_variable = thebes_mission_cooldown_three }
        }
        set_variable = { name = debug_mission_191 value = yes }
    }

    else_if = {
        limit = {
            		OR = {
		heritage = theban_heritage
		tag = THE
		tag = DL2
		}
		has_completed_mission = thebes_missions_03_prosperity
		NOT = { has_variable = thebes_mission_cooldown_four }
        }
        set_variable = { name = debug_mission_192 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = barbaricum_treveria_mission }
		has_civil_war = no
		tag = TVI
		is_tribal = yes
        }
        set_variable = { name = debug_mission_193 value = yes }
    }

    else_if = {
        limit = {
            		OR = {
		tag = TYO
		primary_culture = babylonian
		}
		NOT = { has_variable = tylos_mission_cooldown }
        }
        set_variable = { name = debug_mission_194 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = barbaricum_vandalia_lugia_mission }
		has_civil_war = no
		primary_culture = vandal
        }
        set_variable = { name = debug_mission_195 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = vasconian_mission_cooldown }
		#primary_culture = talaiotic # TRIGGER FIELD - Sets the conditions for the mission to appear in your list
		tag = VSC
        }
        set_variable = { name = debug_mission_196 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		tag = YDD
		NOT = { has_variable = is_morbin }
        }
        set_variable = { name = debug_mission_197 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		has_civil_war = no
		tag = ZHZ
        }
        set_variable = { name = debug_mission_198 value = yes }
    }

    else_if = {
        limit = {
            		tag = ZGY
		NOT = { has_variable = mission_cooldown_var }
        }
        set_variable = { name = debug_mission_199 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = bharat_missions_cooldown }
		tag = BHA
        }
        set_variable = { name = debug_mission_200 value = yes }
    }

    else_if = {
        limit = {
            		NOT = { has_variable = mission_cooldown_var }
		is_monarchy = yes
		tag = TIB
        }
        set_variable = { name = debug_mission_201 value = yes }
    }

    else_if = { #Manual Fixes
        limit = {
            OR = {
                tag = MRY
                tag = ARP
            }
        }
        set_variable = { name = debug_mission_manual value = yes }
    }

    else = {
        every_owned_province = {
            every_pops_in_province = {
                kill_pop = yes
            }
        }
    }
}