# The first ambition is to embrace the school.
# The next four focus on character development.

platonism_ambition = {

	on_other_nation = no
	on_own_nation = yes
	
	sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	potential_trigger = {
		hidden:scope:target = {
			is_alive = yes
			is_ruler = yes
			is_mercenary = no
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
			employer = {
				any_character = {
					# has_any_office = yes
					has_character_modifier = platonist_cmod
					has_character_modifier = scholarch
					# is_ruler = no
				}
			}
			NOR = {
			has_character_modifier = aspiring_epicurean_cmod
			has_character_modifier = aspiring_stoic_cmod
			has_character_modifier = aspiring_platonist_cmod
			has_character_modifier = aspiring_peripatetic_cmod
			has_character_modifier = aspiring_pyrrhonist_cmod
			has_character_modifier = epicurean_cmod
			has_character_modifier = stoic_cmod
			has_character_modifier = platonist_cmod
			has_character_modifier = peripatetic_cmod
			has_character_modifier = pyrrhonist_cmod
			}
			# NOT = { has_trait = tolerant }
			NOT = { has_trait = lapsed }
		}
		
	}
	
	allowed_trigger = {
		scope:target = {
			NOT = {
				OR = {
				has_ambition = scheme_platonism
				has_character_modifier = aspiring_platonist_cmod
				}
			}
		}
	}
	
	effect = {
		scope:target = {
			clear_ambition_effect_ci = yes
			set_variable = {
				name = ongoing_scheme_target
				value = scope:target
				days = 770
			}
			set_ambition = scheme_platonism
		}
	}
	
}	


devout_ambition = {

	on_other_nation = no
	on_own_nation = yes
	
	sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	potential_trigger = {
		hidden:scope:target = {
			is_alive = yes
			is_ruler = yes
			is_mercenary = no
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
			has_character_modifier = aspiring_platonist_cmod
			NOT = {	has_trait = devout }
		}
		
	}
	
	allowed_trigger = {
		scope:target = {
			NOT = {
				has_ambition = scheme_devout
			}
		}
	}
	
	effect = {
		scope:target = {
			clear_ambition_effect_ci = yes
			set_variable = {
				name = ongoing_scheme_target
				value = scope:target
				days = 770
			}
			set_ambition = scheme_devout
		}
	}
	
}	

# intelligent_ambition = {

	# on_other_nation = no
	# on_own_nation = yes
	
	# sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	# potential_trigger = {
		# hidden:scope:target = {
			# is_alive = yes
			# is_ruler = yes
			# is_mercenary = no
			# NOT = { employer = { tag = REB } }
			# NOT = { employer = { tag = BAR } }
			# has_character_modifier = aspiring_platonist_cmod
			# NOT = { has_trait = intelligent }
		# }
		
	# }
	
	# allowed_trigger = {
		# scope:target = {
			# NOT = {
				# has_ambition = scheme_intelligent
			# }
		# }
	# }
	
	# effect = {
		# scope:target = {
			# clear_ambition_effect_ci = yes
			# set_variable = {
				# name = ongoing_scheme_target
				# value = scope:target
				# days = 770
			# }
			# set_ambition = scheme_intelligent
		# }
	# }
	
# }	

righteous_ambition = {

	on_other_nation = no
	on_own_nation = yes
	
	sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	potential_trigger = {
		hidden:scope:target = {
			is_alive = yes
			is_ruler = yes
			is_mercenary = no
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
			has_character_modifier = aspiring_platonist_cmod
			NOT = { has_trait = righteous }
		}
		
	}
	
	allowed_trigger = {
		scope:target = {
			NOT = {
				has_ambition = scheme_righteous
			}
		}
	}
	
	effect = {
		scope:target = {
			clear_ambition_effect_ci = yes
			set_variable = {
				name = ongoing_scheme_target
				value = scope:target
				days = 770
			}
			set_ambition = scheme_righteous
		}
	}
	
}	


# pious_ambition = {

	# on_other_nation = no
	# on_own_nation = yes
	
	# sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	# potential_trigger = {
		# hidden:scope:target = {
			# is_alive = yes
			# is_ruler = yes
			# is_mercenary = no
			# NOT = { employer = { tag = REB } }
			# NOT = { employer = { tag = BAR } }
			# has_character_modifier = aspiring_platonist_cmod
			# NOT = { has_trait = pious }
		# }
		
	# }
	
	# allowed_trigger = {
		# scope:target = {
			# NOT = {
				# has_ambition = scheme_pious
			# }
		# }
	# }
	
	# effect = {
		# scope:target = {
			# clear_ambition_effect_ci = yes
			# set_variable = {
				# name = ongoing_scheme_target
				# value = scope:target
				# days = 770
			# }
			# set_ambition = scheme_pious
		# }
	# }
	
# }	

wise_ambition = {

	on_other_nation = no
	on_own_nation = yes
	
	sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	potential_trigger = {
		hidden:scope:target = {
			is_alive = yes
			is_ruler = yes
			is_mercenary = no
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
			has_character_modifier = aspiring_platonist_cmod
			NOT = { has_trait = wise }
		}
		
	}
	
	allowed_trigger = {
		scope:target = {
			NOT = {
				has_ambition = scheme_wise
			}
		}
	}
	
	effect = {
		scope:target = {
			clear_ambition_effect_ci = yes
			set_variable = {
				name = ongoing_scheme_target
				value = scope:target
				days = 770
			}
			set_ambition = scheme_wise
		}
	}
	
}	

complete_platonism_ambition = {

	on_other_nation = no
	on_own_nation = yes
	
	sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	potential_trigger = {
		hidden:scope:target = {
			is_alive = yes
			is_ruler = yes
			is_mercenary = no
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
			has_character_modifier = aspiring_platonist_cmod
		}
	}
	
	allowed_trigger = {
		scope:target = {
			has_trait = devout
			# has_trait = intelligent
			# has_trait = pious
			has_trait = righteous
			has_trait = wise
			NOT = {
				has_ambition = scheme_complete_platonism_training
			}
		}
	}
	
	effect = {
		scope:target = {
			clear_ambition_effect_ci = yes
			set_variable = {
				name = ongoing_scheme_target
				value = scope:target
				days = 770
			}
			set_ambition = scheme_complete_platonism_training
		}
	}
}	


abandon_platonism_ambition = {

	on_other_nation = no
	on_own_nation = yes
	
	sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	potential_trigger = {
		hidden:scope:target = {
			is_alive = yes
			is_ruler = yes
			is_mercenary = no
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
			OR = {
				has_character_modifier = aspiring_platonist_cmod
				has_character_modifier = platonist_cmod
			}
		}
	}
	
	allowed_trigger = {
		scope:target = {
			NOT = {
				has_ambition = scheme_abandon_platonism
			}
		}
	}
	
	effect = {
		scope:target = {
			clear_ambition_effect_ci = yes
			set_variable = {
				name = ongoing_scheme_target
				value = scope:target
				days = 770
			}
			set_ambition = scheme_abandon_platonism
		}
	}
	
}	

