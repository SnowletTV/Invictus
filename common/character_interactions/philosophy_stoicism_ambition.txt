# The first ambition is to embrace the school.
# The next four focus on character development.

stoicism_ambition = {

	on_other_nation = no
	on_own_nation = yes
	
	sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	potential_trigger = {
		hidden:scope:target = {
			is_alive = yes
			is_ruler = yes
			is_mercenary = no
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
			employer = {
				any_character = {
					# has_any_office = yes
					has_character_modifier = stoic_cmod
					has_character_modifier = scholarch
					# is_ruler = no
				}
			}
			NOR = {
				has_character_modifier = aspiring_epicurean_cmod
				has_character_modifier = aspiring_stoic_cmod
				has_character_modifier = aspiring_platonist_cmod
				has_character_modifier = aspiring_peripatetic_cmod
				has_character_modifier = aspiring_pyrrhonist_cmod
				has_character_modifier = epicurean_cmod
				has_character_modifier = stoic_cmod
				has_character_modifier = platonist_cmod
				has_character_modifier = peripatetic_cmod
				has_character_modifier = pyrrhonist_cmod
			}
			# NOT = { has_trait = tolerant }
			# NOT = { has_trait = lapsed }
		}
		
	}
	
	allowed_trigger = {
		scope:target = {
			NOT = {
				OR = {
				has_ambition = scheme_stoicism
				has_character_modifier = aspiring_stoic_cmod
				}
			}
		}
	}
	
	effect = {
		scope:target = {
			clear_ambition_effect_ci = yes
			set_variable = {
				name = ongoing_scheme_target
				value = scope:target
				days = 770
			}
			set_ambition = scheme_stoicism
		}
	}
	
}	


# guileless_ambition = {

	# on_other_nation = no
	# on_own_nation = yes
	
	# sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	# potential_trigger = {
		# hidden:scope:target = {
			# is_alive = yes
			# is_ruler = yes
			# is_mercenary = no
			# NOT = { employer = { tag = REB } }
			# NOT = { employer = { tag = BAR } }
			# has_character_modifier = aspiring_stoic_cmod
			# NOT = {	has_trait = guileless }
		# }
		
	# }
	
	# allowed_trigger = {
		# scope:target = {
			# NOT = {
				# has_ambition = scheme_guileless
			# }
		# }
	# }
	
	# effect = {
		# scope:target = {
			# clear_ambition_effect_ci = yes
			# set_variable = {
				# name = ongoing_scheme_target
				# value = scope:target
				# days = 770
			# }
			# set_ambition = scheme_guileless
		# }
	# }
	
# }	

plain_speaking_ambition = {

	on_other_nation = no
	on_own_nation = yes
	
	sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	potential_trigger = {
		hidden:scope:target = {
			is_alive = yes
			is_ruler = yes
			is_mercenary = no
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
			has_character_modifier = aspiring_stoic_cmod
			NOT = { has_trait = plain_speaking }
		}
		
	}
	
	allowed_trigger = {
		scope:target = {
			NOT = {
				has_ambition = scheme_plain_speaking
			}
		}
	}
	
	effect = {
		scope:target = {
			clear_ambition_effect_ci = yes
			set_variable = {
				name = ongoing_scheme_target
				value = scope:target
				days = 770
			}
			set_ambition = scheme_plain_speaking
		}
	}
	
}	

# righteous_ambition = {

	# on_other_nation = no
	# on_own_nation = yes
	
	# sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	# potential_trigger = {
		# hidden:scope:target = {
			# is_alive = yes
			# is_ruler = yes
			# is_mercenary = no
			# NOT = { employer = { tag = REB } }
			# NOT = { employer = { tag = BAR } }
			# has_character_modifier = aspiring_stoic_cmod
			# NOT = { has_trait = righteous }
		# }
		
	# }
	
	# allowed_trigger = {
		# scope:target = {
			# NOT = {
				# has_ambition = scheme_righteous
			# }
		# }
	# }
	
	# effect = {
		# scope:target = {
			# clear_ambition_effect_ci = yes
			# set_variable = {
				# name = ongoing_scheme_target
				# value = scope:target
				# days = 770
			# }
			# set_ambition = scheme_righteous
		# }
	# }
	
# }	
self_controlled_ambition = {

	on_other_nation = no
	on_own_nation = yes
	
	sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	potential_trigger = {
		hidden:scope:target = {
			is_alive = yes
			is_ruler = yes
			is_mercenary = no
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
			has_character_modifier = aspiring_stoic_cmod
			NOT = { has_trait = self_controlled }
		}
		
	}
	
	allowed_trigger = {
		scope:target = {
			NOT = {
				has_ambition = scheme_self_controlled
			}
		}
	}
	
	effect = {
		scope:target = {
			clear_ambition_effect_ci = yes
			set_variable = {
				name = ongoing_scheme_target
				value = scope:target
				days = 770
			}
			set_ambition = scheme_self_controlled
		}
	}
	
}	

secure_ambition = {

	on_other_nation = no
	on_own_nation = yes
	
	sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	potential_trigger = {
		hidden:scope:target = {
			is_alive = yes
			is_ruler = yes
			is_mercenary = no
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
			has_character_modifier = aspiring_stoic_cmod
			NOT = { has_trait = secure }
		}
		
	}
	
	allowed_trigger = {
		scope:target = {
			NOT = {
				has_ambition = scheme_secure
			}
		}
	}
	
	effect = {
		scope:target = {
			clear_ambition_effect_ci = yes
			set_variable = {
				name = ongoing_scheme_target
				value = scope:target
				days = 770
			}
			set_ambition = scheme_secure
		}
	}
	
}	

complete_stoicism_ambition = {

	on_other_nation = no
	on_own_nation = yes
	
	sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	potential_trigger = {
		hidden:scope:target = {
			is_alive = yes
			is_ruler = yes
			is_mercenary = no
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
			has_character_modifier = aspiring_stoic_cmod
		}
	}
	
	allowed_trigger = {
		scope:target = {
			has_trait = plain_speaking
			has_trait = secure
			has_trait = self_controlled
			NOT = {
				has_ambition = scheme_complete_stoicism_training
			}
		}
	}
	
	effect = {
		scope:target = {
			clear_ambition_effect_ci = yes
			set_variable = {
				name = ongoing_scheme_target
				value = scope:target
				days = 770
			}
			set_ambition = scheme_complete_stoicism_training
		}
	}
}	


abandon_stoicism_ambition = {

	on_other_nation = no
	on_own_nation = yes
	
	sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"
	
	potential_trigger = {
		hidden:scope:target = {
			is_alive = yes
			is_ruler = yes
			is_mercenary = no
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
			OR = {
				has_character_modifier = aspiring_stoic_cmod
				has_character_modifier = stoic_cmod
			}
		}
	}
	
	allowed_trigger = {
		scope:target = {
			NOT = {
				has_ambition = scheme_abandon_stoicism
			}
		}
	}
	
	effect = {
		scope:target = {
			clear_ambition_effect_ci = yes
			set_variable = {
				name = ongoing_scheme_target
				value = scope:target
				days = 770
			}
			set_ambition = scheme_abandon_stoicism
		}
	}
	
}	

