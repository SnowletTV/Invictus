cappadocia_mission_inv = {
	icon = general_1
	header = mission_image_athens

	repeatable = no

	chance = {
		factor = 10
	}

	ai_chance = { factor = 10 }

	potential = {
		tag = CAP
	}

	abort = {}

	on_start = {}

	on_completion = {}

	on_abort = {}

	cappadocia_mission_inv_1_task = {  #Stabilize Our Position
		icon = task_political

		highlight = {
			scope:province = {
				OR = {
					root.any_owned_province = {
						any_pops_in_province = {
							pop_hapiness < 0.5
						}
					}
				}
			}
		}

		allow = {
			invention = global_cohort_recruit_speed_inv
			custom_tooltip = {
				text = cappadocia_fort_svalue_tt
				cappadocia_fort_svalue >= 10
			}
			any_owned_province = {
				count = all
				any_pops_in_province = {
					count = all
					pop_hapiness >= 0.5
				}
			}
		}

		on_completion = {
			add_political_influence = 100
			add_treasury = 300
			add_manpower = 10 #1 Manpower = 500 Manpower in the game
		}
	}

	### Branch 1 - Loyal Vassal
	cappadocia_mission_inv_2_task = { #Strength with the Antigonids

		icon = task_diplomatic

		requires = {cappadocia_mission_inv_1_task}

		allow = { c:ANT = { opinion = { target = root value >= 90 } }
            current_ruler = { is_friend = c:ANT.current_ruler }}

		on_completion = {}
	}

	cappadocia_mission_inv_3_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

	cappadocia_mission_inv_4_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

	cappadocia_mission_inv_5_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

	cappadocia_mission_inv_6_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

	cappadocia_mission_inv_7_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

	cappadocia_mission_inv_8_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

	cappadocia_mission_inv_9_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

	cappadocia_mission_inv_10_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

	cappadocia_mission_inv_11_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

	cappadocia_mission_inv_12_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

	cappadocia_mission_inv_13_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

	cappadocia_mission_inv_14_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

	### Branch 2 - xxx
	cappadocia_mission_inv_15_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

	cappadocia_mission_inv_16_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

	cappadocia_mission_inv_17_task = {
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

}
    ### Branch 1 - Developing Cappadocia
	cappadocia_mission_inv_16_task = {
		icon = task_economical

		requires = {}

		allow = {}

		on_completion = {}
	}
