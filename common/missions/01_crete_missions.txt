crete_mission_inv = {
	icon = generic_greek_1
	header = mission_image_generic_greek

	repeatable = no

	chance = {
		factor = 10
	}

	ai_chance = { factor = 10 }

	potential = {
    	primary_culture = cretan
    	NOT = { has_variable = crete_mission_cooldown }	
		NOT = { tag = 1KR }	
    }

	abort = {
		any_country = { tag = 1KR }
	}

	on_start = {
    	start_mission_ai_effect = yes
	    
    }

    on_abort = {
        custom_tooltip = general_mission_cooldown_tt
        set_variable = {
            name = crete_mission_cooldown
            days = 7300
        }
    }
 
	on_completion = {
		add_1_free_province_investments = yes
        complete_mission_effect = yes
    }

crete_mission_inv_1_task = { #Discipline in the Camps
		icon = task_battle

		requires = {}

		allow = {
			treasury >= 25
			political_influence >= 20
		}

		on_completion = {
			trigger_event = me_crete_inv.1
			add_treasury = -25
			add_political_influence = -20
			add_country_modifier = {
				name = "grumbling_soldiers"
				duration = 1850
			}
		}
	}

crete_mission_inv_2_task = { #Iron Weapons
		icon = task_expansion

		requires = {}

		allow = {
			treasury >= 50
			capital_scope = {
				is_importing_trade_good = iron
				is_importing_trade_good = leather
                }
			}

		on_completion = {
			add_treasury = -50
			trigger_event = me_crete_inv.2
			add_country_modifier = {
				name = "iron_weapons"
				duration = 1835
			}
		}
	}

crete_mission_inv_3_task = { #An Unruly State of Affairs
		icon = task_political
		duration = 180

		requires = {}

		highlight = {
			scope:province = {
				is_in_area = crete_area
			}
        }

		allow = {
			treasury >= 25
			political_influence >= 15
		}

		on_completion = {
			trigger_event = me_crete_inv.3
			add_political_influence = -20
        	add_treasury = -25
			area:crete_area = {
                add_provincial_claim_effect = yes
			}
		}
	}

crete_mission_inv_4_task = { #Siegeworks and Sappers
		icon = task_conquest
		duration = 180

		requires = {crete_mission_inv_1_task}

		allow = {
			military_experience >= 5
			treasury >= 50
			political_influence >= 30
		}

		on_completion = {
			trigger_event = me_crete_inv.4
			add_military_experience = -5
			add_treasury = -50
			add_political_influence = -30
			add_country_modifier = {
				name = "cretan_sappers"
				duration = 1850
			}
		}
	}

crete_mission_inv_5_task = { #A Gold Coin to the First Man
		icon = task_expansion

		requires = {crete_mission_inv_1_task}

		allow = {
			treasury >= 50
			political_influence >= 20
		}

		on_completion = {
			trigger_event = me_crete_inv.5
			add_treasury = -50
			add_political_influence = -20
			add_country_modifier = {
				name = "motivated_soldiers"
				duration = 1850
			}
		}
	}

crete_mission_inv_6_task = { #Squeezing Every Last Drop
		icon = task_economical
		duration = 365

		requires = {crete_mission_inv_4_task crete_mission_inv_5_task}

		allow = {
			treasury >= 100
			political_influence >= 20
		}

		on_completion = {
			trigger_event = me_crete_inv.6
			add_treasury = -100
			add_political_influence = -20
			add_stability = -5
			add_manpower = 5
		}
	}

crete_mission_inv_7_task = {
		icon = task_happiness

		requires = {}

		allow = {
			capital_scope = {
				is_importing_trade_good = grain
                }
			}

		on_completion = {
			trigger_event = me_crete_inv.7
			add_country_modifier = {
				name = "grumbling_bellies"
				duration = 1850
			}
		}
	}

crete_mission_inv_8_task = { #Conquering the Unconquerable
		icon = task_battle

		requires = {crete_mission_inv_2_task crete_mission_inv_7_task}

		highlight = {
			scope:province = {
				is_in_area = crete_area
			}
        }

		allow = {
			custom_tooltip = {
			text = thicc_crete_tt	
        	any_owned_province = {
        		count >= 8
        		is_in_area = crete_area
        	}
        }
	}
		

		on_completion = {
			trigger_event = me_crete_inv.8
			current_ruler = {
				add_trait = conqueror
			}
			add_1_free_province_investments = yes	
		}
	}

crete_mission_inv_9_task = { #Bull Leapers
		icon = task_religion
		duration = 90

		requires = {crete_mission_inv_8_task}

		allow = {
			owns = 358
			treasury >= 25
			political_influence >= 20
		}

		highlight = {
			scope:province = {
				province_id = 358
			}
        }

		on_completion = {
			trigger_event = me_crete_inv.9
			add_treasury = -25
			add_political_influence = -20
			current_ruler = {
				add_zeal = 1
			}
		}
	}

crete_mission_inv_10_task = {
		icon = task_heracles
		final = yes

		requires = {crete_mission_inv_9_task}

		allow = {
			owns = 358
			custom_tooltip = {
				text = own_crete_tt
				any_owned_province = {
        		count >= 12
        		is_in_area = crete_area
        	}
			}
		}

		highlight = {
			scope:province = {
				province_id = 358
			}
        }

		on_completion = {
			trigger_event = me_crete_inv.10
			set_capital = p:358
			add_country_modifier = {
				name = "mighty_crete"
				duration = -1
			}
		}
	}

