crete_mission_inv = {
	icon = generic_greek_1
	header = mission_image_generic_greek

	repeatable = no

	chance = {
		factor = 10
	}

	ai_chance = { factor = 10 }

	potential = {
    	primary_culture = cretan
    	NOT = { has_variable = crete_mission_cooldown }
    }

	abort = {}

	on_start = {
    	start_mission_ai_effect = yes
	    
    }

    on_abort = {
        custom_tooltip = general_mission_cooldown_tt
        set_variable = {
            name = crete_mission_cooldown
            days = 7300
        }
    }
 
	on_completion = {
		add_3_free_province_investments = yes
        complete_mission_effect = yes
    }

crete_mission_inv_1_task = { #Discipline in the Camps
		icon = task_battle

		requires = {}

		allow = {
			treasury >= 25
			political_influence >= 20
		}

		on_completion = {}
	}

crete_mission_inv_2_task = { #Iron Weapon
		icon = task_expansion

		requires = {}

		allow = {}

		on_completion = {}
	}

crete_mission_inv_3_task = { #An Unruly State of Affairs
		icon = task_political
		duration = 90

		requires = {}

		allow = {
			treasury >= 25
			political_influence >= 15
		}

		on_completion = {
			add_political_influence = -20
        	add_treasury = -25
			duration = -1
			area:crete_area = {
                add_provincial_claim_effect = yes
			}
		}
	}

crete_mission_inv_4_task = {
		icon = task_conquest

		requires = {crete_mission_inv_1_task}

		allow = {}

		on_completion = {}
	}

crete_mission_inv_5_task = {
		icon = task_expansion

		requires = {crete_mission_inv_1_task}

		allow = {}

		on_completion = {}
	}

crete_mission_inv_6_task = {
		icon = task_happiness

		requires = {crete_mission_inv_4_task crete_mission_inv_5_task}

		allow = {}

		on_completion = {}
	}

crete_mission_inv_7_task = {
		icon = task_happiness

		requires = {}

		allow = {}

		on_completion = {}
	}

crete_mission_inv_8_task = {
		icon = task_expansion

		requires = {crete_mission_inv_2_task crete_mission_inv_7_task}

		allow = {
			custom_tooltip = {
			text = thicc_crete_tt	
        	any_owned_province = {
        		count >= 5
        		is_in_area = crete_area
        	}
        }
	}
		

		on_completion = {}
	}

crete_mission_inv_9_task = {
		icon = task_religion

		requires = {crete_mission_inv_8_task}

		allow = {}

		on_completion = {}
	}

crete_mission_inv_10_task = {
		icon = task_heracles

		requires = {crete_mission_inv_9_task}

		allow = {}

		on_completion = {}
	}

