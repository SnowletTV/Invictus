fezzani_mission_01 = {
    header = "mission_image_general" # SPECIAL FIELD - Sets top picture for the mission
    icon = "general_1" # SPECIAL FIELD - Sets icon for the mission

    repeatable = no # SPECIAL FIELD - Sets if a mission can be done once or several times

    chance = {
        factor = 50000
        } # SPECIAL FIELD - Sets the chances of a mission appearing in your list of available missions

    potential = {
        NOT = { has_variable = fezzani_mission_01 }
        OR = {
            tag = MKA # MAKAE
            tag = PSY # PSYLLI
            tag = NAS # NASAMONES
            tag = GAR # GARAMANTES
            tag = MHY # MACHYLES
        } # TRIGGER FIELD - Sets the conditions for the mission to appear in your list
        is_subject = no # must not be a subject
    }
    on_start = {
    save_scope_as = mission_country
    } # EFFECT FIELD - These effects are run when the mission is started

    on_abort = {
        custom_tooltip = general_mission_cooldown_tt
        set_variable = {
            name = fezzani_mission_cooldown
            days = 1 # 7300
            }
			if = {
				limit = { has_variable = fezzani_domination }
				remove_variable = fezzani_domination
			}
            if = {
                limit = { has_variable = fezzani_peace }
                remove_variable = fezzani_peace
            }
    } # EFFECT FIELD - These effects are run when the mission is aborted

    on_completion = {
        add_political_influence = 150
        add_stability = 20
        if = {
            limit = { has_variable = fezzani_domination }
            remove_variable = fezzani_domination
        }
        if = {
            limit = { has_variable = fezzani_peace }
            remove_variable = fezzani_peace
        }
    } # EFFECT FIELD - These effects are run when the mission is completed

    fezzani_mission_01_task_01 = { # task 1 - The People of Fezzan
        icon = "task_diplomatic"

        duration = 1 # 180 days

        on_start = {
            trigger_event = {
                id = me_fezzani.1
                days = { 80 160 } 
            }
        }
    }

    fezzani_mission_01_task_02 = { # task 2 - Establish peace in Fezzan 
        requires = { fezzani_mission_01_task_01 }
        # prevented_by = { fezzani_mission_01_task_10 fezzani_mission_01_task_11 fezzani_mission_01_task_12 }
        bypass = { has_completed_mission_task = fezzani_mission_01_task_02 }
        icon = "task_diplomatic"
        allow = {
            political_influence >= 25
        }

        on_start = {
            trigger_event = {
                id = me_fezzani.2
                days = { 15 75 }
            }
        }
    }

    fezzani_mission_01_task_03 = { # task 3 leader of the fezzani
        requires = { fezzani_mission_01_task_02 }
        # prevented_by = { fezzani_mission_01_task_10 fezzani_mission_01_task_11 fezzani_mission_01_task_12 }
        bypass = { has_completed_mission_task = fezzani_mission_01_task_03 }
        icon = "task_diplomatic"
        allow = {
            any_subject = { # check if root country has 4 or more subjects with primary culture in the fezzani culture group
				count = 4
				country_culture_group = fezzani
			}	 
        }

        on_start = {
            trigger_event = {
                id = me_fezzani.3
                days = { 1 15 }
            }
        }
    }

    fezzani_mission_01_task_04 = { # Task 4 - Send an Envoy to the Great Powers
        requires = { fezzani_mission_01_task_02 }
        # prevented_by = { fezzani_mission_01_task_10 fezzani_mission_01_task_11 fezzani_mission_01_task_12 }
        bypass = { has_completed_mission_task = fezzani_mission_01_task_04 }
        icon = "task_diplomatic"
        allow = { 
            political_influence >= 50
            diplomatic_relations = 4
        }

        on_start = { 
            trigger_event = { 
                id = me_fezzani.4 # reference scythia_mission_01_greek_stance_task_2
                days = 365
            }
        }
    }

    fezzani_mission_01_task_05 = { # Task 5 - Befriend the Garamantes
        requires = { fezzani_mission_01_task_02 }
        # prevented_by = { fezzani_mission_01_task_10 fezzani_mission_01_task_11 fezzani_mission_01_task_12 }
        bypass = { has_completed_mission_task = fezzani_mission_01_task_05 }
        icon = "task_conquest"
        potential = { tag = NAS }
        allow = {
            tag = NAS
            political_influence >= 50
			custom_tooltip = {
				# text = syracuse_4_1_allow_tt
				any_owned_province = {
                    count >= 15
                }
                tag = GAR
				is_subject = no
				NOR = {
					alliance_with = root
					war_with = root
					opinion = { target = root value <= -25 }
				} 
			}
        }

        highlight = {
            is_in_region = fezzan_region
            owner = {
                   country_culture_group = fezzani
            }
        }

        on_start = { 
            trigger_event = { 
                id = me_fezzani.5 
                days = 365
            }
        }
    }

    fezzani_mission_01_task_06 = { # Task 6 - Peace in Fezzan
        requires = { fezzani_mission_01_task_02 }
        # prevented_by = { fezzani_mission_01_task_13 fezzani_mission_01_task_14 fezzani_mission_01_task_15 }
        bypass = {
            has_variable = fezzani_domination
        }
        icon = "task_conquest"
        allow = {
            owns_or_subject_owns_area = syrtis_area
            owns_or_subject_owns_area = psyllia_area
            owns_or_subject_owns_area = shatti_area
        }

        highlight = {
            scope:province = {
                OR = {
                    is_in_area = syrtis_area
                    is_in_area = psyllia_area
                    is_in_area = shatti_area
                }
            }
        }
        on_completion = {
            set_variable = fezzani_peace
        }
    }

    fezzani_mission_01_task_07 = { # task 7 - peaceful consolidate fezzan
        requires = { fezzani_mission_01_task_06 }
        # prevented_by = { fezzani_mission_01_task_13 fezzani_mission_01_task_14 fezzani_mission_01_task_15 }
        bypass = {
            has_variable = fezzani_domination
        }
        icon = "task_diplomatic"
        allow = {
            owns_or_subject_owns_area = nasamonia_area
            owns_or_subject_owns_area = syrtis_area
            owns_or_subject_owns_area = shatti_area
            owns_or_subject_owns_area = hayaa_area
            owns_or_subject_owns_area = murzuq_area
            owns_or_subject_owns_area = himka_area
            owns_or_subject_owns_area = psyllia_area
            owns_or_subject_owns_area = zamzam_area
            owns_or_subject_owns_area = girgiri_area
            owns_or_subject_owns_area = soffegin_area
            owns_or_subject_owns_area = phazania_area
        }

        highlight = {
        	scope:province = {
                OR = {
                    is_in_area = nasamonia_area
                    is_in_area = syrtis_area
                    is_in_area = shatti_area
                    is_in_area = hayaa_area
                    is_in_area = murzuq_area
                    is_in_area = himka_area
                    is_in_area = psyllia_area
                    is_in_area = zamzam_area
                    is_in_area = girgiri_area
                    is_in_area = soffegin_area
                    is_in_area = phazania_area
                }
            }
        }
    }

    fezzani_mission_01_task_08 = { # The kingdom of Garamantes
        icon = "task_conquest"
        potential = { tag = GAR }
        allow = {
            tag = GAR
        }

        on_start = { 
            trigger_event = { 
                id = me_fezzani.8
                days = { 55 150 }
            }
        }
    }

    fezzani_mission_01_task_09 = { # The Nasamones
        icon = "task_diplomatic"
        potential = { tag = NAS }
        allow = {
            tag = NAS
        }

        on_start = { 
            trigger_event = { 
                id = me_fezzani.9
                days = { 77 140 }
            }
        }
    }

    fezzani_mission_01_task_10 = { # Conquer our neighbors - Garamantes
        requires = { fezzani_mission_01_task_08 }
        icon = "task_conquest"
        potential = { tag = GAR }
        allow = {
            tag = GAR
            treasury >= 100
            political_influence >= 100
        }

        on_start = {
            trigger_event = { # get claims on neighboring provinces
                id = fezzani.10
                days = 1
            }
        }
    }

    fezzani_mission_01_task_13 = { # Garamantes Fezzani Domination
        requires = { fezzani_mission_01_task_10 }
        icon = "task_conquest"
        bypass = {
            has_variable = fezzani_peace
        }
        potential = { tag = GAR }
        allow = {
            tag = GAR
        }

        on_start = {
            trigger_event = { # get claims on neighboring provinces
                # id = fezzani.13
                days = { 180 365 }
            }
        }
        on_completion = {
            set_variable = fezzani_domination
        }
    }

    fezzani_mission_01_task_11 = { # Conquer our neighbors - nasamones
        requires = { fezzani_mission_01_task_09 }
        icon = "task_conquest"
        potential = { tag = NAS }
        allow = {
            tag = NAS
            treasury >= 100
            political_influence >= 100
        }

        on_start = {
            trigger_event = { # get claims on neighboring provinces
                id = fezzani.10
                days = 1
            }
        }
    }

    fezzani_mission_01_task_14 = { # nasamones Fezzani Domination
        requires = { fezzani_mission_01_task_11 }
        potential = { tag = NAS }
        bypass = {
            has_variable = fezzani_peace
        }
        icon = "task_conquest"

        allow = {
            tag = NAS
        }

        on_start = { 
            trigger_event = { # get claims on the remaining region
                # id = me_fezzani.11
                days = { 180 365 }
            }
        }
        on_completion = {
            set_variable = fezzani_domination
        }
    }

    fezzani_mission_01_task_12 = { # Conquer our neighbors - everyone else
        requires = { fezzani_mission_01_task_01 }
        icon = "task_conquest"
        potential = { 
            OR = {
                tag = MKA # MAKAE
                tag = PSY # PSYLLI
                tag = MHY # MACHYLES
            }
            NOT = {
                tag = NAS 
                tag = GAR
            }
        }
        allow = {
            OR = {
                tag = MKA # MAKAE
                tag = PSY # PSYLLI
                tag = MHY # MACHYLES
            }
            treasury >= 100
            political_influence >= 100
        }

        on_start = {
            trigger_event = { # get claims on neighboring provinces
                id = fezzani.10
                days = 1
            }
        }
    }

    fezzani_mission_01_task_15 = { # Fezzani Domination - everyone else
        requires = { fezzani_mission_01_task_12 }
        icon = "task_conquest"
        bypass = {
            has_variable = fezzani_peace
        }
        potential = { 
            OR = {
                tag = MKA # MAKAE
                tag = PSY # PSYLLI
                tag = MHY # MACHYLES
            }
            NOT = {
                tag = NAS 
                tag = GAR
            }
        }

        allow = {
            OR = {
                tag = MKA
                tag = PSY
                tag = MHY
            }
        }

        on_start = { 
            trigger_event = { # get claims on the remaining region
                # id = me_fezzani.11
                days = 1 # { 180 365 }
            }
        }
        on_completion = {
            set_variable = fezzani_domination
        }
    }

    fezzani_mission_01_task_16 = { # consolidate fezzan - garamantes
        requires = { fezzani_mission_01_task_13 }
        bypass = {
            has_variable = fezzani_peace
        }
        potential = { tag = GAR }
        icon = "task_conquest"
        allow = {
            owns_or_subject_owns_area = nasamonia_area
            owns_or_subject_owns_area = syrtis_area
            owns_or_subject_owns_area = shatti_area
            owns_or_subject_owns_area = hayaa_area
            owns_or_subject_owns_area = murzuq_area
            owns_or_subject_owns_area = himka_area
            owns_or_subject_owns_area = psyllia_area
            owns_or_subject_owns_area = zamzam_area
            owns_or_subject_owns_area = girgiri_area
            owns_or_subject_owns_area = soffegin_area
            owns_or_subject_owns_area = phazania_area
        }

        highlight = {
        	scope:province = {
                OR = {
                    is_in_area = nasamonia_area
                    is_in_area = syrtis_area
                    is_in_area = shatti_area
                    is_in_area = hayaa_area
                    is_in_area = murzuq_area
                    is_in_area = himka_area
                    is_in_area = psyllia_area
                    is_in_area = zamzam_area
                    is_in_area = girgiri_area
                    is_in_area = soffegin_area
                    is_in_area = phazania_area
                }
            }
        }
    }

    fezzani_mission_01_task_17 = { # consolidate fezzan - nasamones
        requires = { fezzani_mission_01_task_14 }
        bypass = {
            has_variable = fezzani_peace
        }
        potential = { tag = NAS }
        icon = "task_conquest"
        allow = {
            owns_or_subject_owns_area = nasamonia_area
            owns_or_subject_owns_area = syrtis_area
            owns_or_subject_owns_area = shatti_area
            owns_or_subject_owns_area = hayaa_area
            owns_or_subject_owns_area = murzuq_area
            owns_or_subject_owns_area = himka_area
            owns_or_subject_owns_area = psyllia_area
            owns_or_subject_owns_area = zamzam_area
            owns_or_subject_owns_area = girgiri_area
            owns_or_subject_owns_area = soffegin_area
            owns_or_subject_owns_area = phazania_area
        }

        highlight = {
        	scope:province = {
                OR = {
                    is_in_area = nasamonia_area
                    is_in_area = syrtis_area
                    is_in_area = shatti_area
                    is_in_area = hayaa_area
                    is_in_area = murzuq_area
                    is_in_area = himka_area
                    is_in_area = psyllia_area
                    is_in_area = zamzam_area
                    is_in_area = girgiri_area
                    is_in_area = soffegin_area
                    is_in_area = phazania_area
                }
            }
        }
    }

    fezzani_mission_01_task_18 = { # consolidate fezzan - everyone else
        requires = { fezzani_mission_01_task_15 }
        icon = "task_conquest"
        bypass = {
            has_variable = fezzani_peace
        }
        potential = { 
            OR = {
                tag = MKA # MAKAE
                tag = PSY # PSYLLI
                tag = MHY # MACHYLES
            }
            NOT = {
                tag = NAS 
                tag = GAR
            }
        }

        allow = {
            OR = {
                tag = MKA
                tag = PSY
                tag = MHY
            }
            owns_or_subject_owns_area = nasamonia_area
            owns_or_subject_owns_area = syrtis_area
            owns_or_subject_owns_area = shatti_area
            owns_or_subject_owns_area = hayaa_area
            owns_or_subject_owns_area = murzuq_area
            owns_or_subject_owns_area = himka_area
            owns_or_subject_owns_area = psyllia_area
            owns_or_subject_owns_area = zamzam_area
            owns_or_subject_owns_area = girgiri_area
            owns_or_subject_owns_area = soffegin_area
            owns_or_subject_owns_area = phazania_area
        }

        highlight = {
        	scope:province = {
                OR = {
                    is_in_area = nasamonia_area
                    is_in_area = syrtis_area
                    is_in_area = shatti_area
                    is_in_area = hayaa_area
                    is_in_area = murzuq_area
                    is_in_area = himka_area
                    is_in_area = psyllia_area
                    is_in_area = zamzam_area
                    is_in_area = girgiri_area
                    is_in_area = soffegin_area
                    is_in_area = phazania_area
                }
            }
        }
    }

    fezzani_mission_01_task_19 = { # forming fezzan as garamantes 
        requires = { fezzani_mission_01_task_07 fezzani_mission_01_task_16 }
        potential = { tag = GAR }
        icon = "task_conquest"
        final = yes
        allow = {
            tag = GAR
            owns_or_subject_owns_area = nasamonia_area
            owns_or_subject_owns_area = syrtis_area
            owns_or_subject_owns_area = shatti_area
            owns_or_subject_owns_area = hayaa_area
            owns_or_subject_owns_area = murzuq_area
            owns_or_subject_owns_area = himka_area
            owns_or_subject_owns_area = psyllia_area
            owns_or_subject_owns_area = zamzam_area
            owns_or_subject_owns_area = girgiri_area
            owns_or_subject_owns_area = soffegin_area
            owns_or_subject_owns_area = phazania_area
        }

        highlight = {
        	scope:province = {
                OR = {
                    is_in_area = nasamonia_area
                    is_in_area = syrtis_area
                    is_in_area = shatti_area
                    is_in_area = hayaa_area
                    is_in_area = murzuq_area
                    is_in_area = himka_area
                    is_in_area = psyllia_area
                    is_in_area = zamzam_area
                    is_in_area = girgiri_area
                    is_in_area = soffegin_area
                    is_in_area = phazania_area
                }
            }
        }
        on_completion = {
            remove_variable = fezzan_domination
        }
    }

    fezzani_mission_01_task_20 = { # forming fezzan as nasamones 
        requires = { fezzani_mission_01_task_07 fezzani_mission_01_task_17 }
        potential = { tag = NAS }
        icon = "task_conquest"
        final = yes
        allow = {
            owns_or_subject_owns_area = nasamonia_area
            owns_or_subject_owns_area = syrtis_area
            owns_or_subject_owns_area = shatti_area
            owns_or_subject_owns_area = hayaa_area
            owns_or_subject_owns_area = murzuq_area
            owns_or_subject_owns_area = himka_area
            owns_or_subject_owns_area = psyllia_area
            owns_or_subject_owns_area = zamzam_area
            owns_or_subject_owns_area = girgiri_area
            owns_or_subject_owns_area = soffegin_area
            owns_or_subject_owns_area = phazania_area
        }

        highlight = {
        	scope:province = {
                OR = {
                    is_in_area = nasamonia_area
                    is_in_area = syrtis_area
                    is_in_area = shatti_area
                    is_in_area = hayaa_area
                    is_in_area = murzuq_area
                    is_in_area = himka_area
                    is_in_area = psyllia_area
                    is_in_area = zamzam_area
                    is_in_area = girgiri_area
                    is_in_area = soffegin_area
                    is_in_area = phazania_area
                }
            }
        }
        on_completion = {
            remove_variable = fezzan_domination
        }
    }

    fezzani_mission_01_task_21 = { # forming fezzan as everyone else 
        requires = { fezzani_mission_01_task_07 fezzani_mission_01_task_18 }
        potential = { 
            OR = {
                tag = MKA # MAKAE
                tag = PSY # PSYLLI
                tag = MHY # MACHYLES
            }
            NOT = {
                tag = NAS 
                tag = GAR
            }
        }
        icon = "task_conquest"
        final = yes
        allow = {
            OR = {
                tag = MKA
                tag = PSY
                tag = MHY
            }
            owns_or_subject_owns_area = nasamonia_area
            owns_or_subject_owns_area = syrtis_area
            owns_or_subject_owns_area = shatti_area
            owns_or_subject_owns_area = hayaa_area
            owns_or_subject_owns_area = murzuq_area
            owns_or_subject_owns_area = himka_area
            owns_or_subject_owns_area = psyllia_area
            owns_or_subject_owns_area = zamzam_area
            owns_or_subject_owns_area = girgiri_area
            owns_or_subject_owns_area = soffegin_area
            owns_or_subject_owns_area = phazania_area
        }

        highlight = {
        	scope:province = {
                OR = {
                    is_in_area = nasamonia_area
                    is_in_area = syrtis_area
                    is_in_area = shatti_area
                    is_in_area = hayaa_area
                    is_in_area = murzuq_area
                    is_in_area = himka_area
                    is_in_area = psyllia_area
                    is_in_area = zamzam_area
                    is_in_area = girgiri_area
                    is_in_area = soffegin_area
                    is_in_area = phazania_area
                }
            }
        }
        on_completion = {
            remove_variable = fezzan_domination
        }
    }

    fezzani_mission_01_task_22 = { # The City of Garama
        icon = "task_diplomatic"
        potential = {  tag = GAR }
        allow = {
            tag = GAR
            owns = 8168 # Garama
        }

        highlight = {
            scope:province = {
                province_id = 8168
            }
        }
    }

    fezzani_mission_01_task_23 = { # Cities of Garamantes
        requires = { fezzani_mission_01_task_20 }
        icon = "task_diplomatic"
        potential = {  tag = GAR }
        allow = {
            tag = GAR
            owns = 8168 # Garama
        }

        highlight = {
            scope:province = {
                province_id = 8163
            }
        }
    }

    fezzani_mission_01_task_24 = {
        icon = "task_diplomatic"

		allow = {
			treasury >= 200
			trigger_if = {
				any_state_province = {
					    is_importing_trade_good = dates
				}
			}
		}

        on_start = {
        }
    }

}