 nuragic_mission_inv = {
	icon = carthage_1
	header = german_mission1

	repeatable = no

	chance = {
		factor = 10
	}

	ai_chance = { factor = 10 }

	potential = {
    	primary_culture = nuragic
    	NOT = { has_variable = nuragic_mission_cooldown }
    }

	abort = {}

	on_start = {
    	start_mission_ai_effect = yes
	    trigger_event = me_nuragic_inv.15
    }

    on_abort = {
        custom_tooltip = general_mission_cooldown_tt
        set_variable = {
            name = nuragic_mission_cooldown
            days = 7300
        }
    }
 
	on_completion = {
		add_3_free_province_investments = yes
        complete_mission_effect = yes
    }


	nuragic_mission_inv_1_task = { #Gathering the Tribal Elders
		icon = task_expansion
		duration = 60

		allow = {
        	treasury >= 25
			political_influence >= 15
        }
        on_start = {
        	add_political_influence = -15
        	add_treasury = -25
        }
			

		on_completion = {
			hidden:custom_tooltip = me_nuragic_1_tooltip
            trigger_event = me_nuragic_inv.1
        }
	}

	nuragic_mission_inv_2_task = { #Securing the Reserves
		icon = task_conquest

		allow = {
			capital_scope = {
				is_importing_trade_good = grain
                }
				custom_tooltip = {
					text = securing_reserves_tt
        			any_owned_province = {
        			count >= 1
        			has_province_rank = settlement
        			num_of_basic_settlement_infratructure_building = 1
				}
        	}
        }

		on_completion = {
			trigger_event = me_nuragic_inv.2
		}
	}

	nuragic_mission_inv_3_task = { #Restore the Nuraghe
		icon = task_diplomatic
		duration = 365

		allow = {
			treasury >= 40
			capital_scope.state = {
	                calc_true_if = {
	                    amount >= 1
	                    state_improvement_military_trigger = yes           
	                }
	            }
	        }
        

		on_completion = {
			trigger_event = me_nuragic_inv.3
			add_treasury = -40
			add_country_modifier = {
				name = "nuraghe_network"
				duration = 14600
			}
		}
	}

	nuragic_mission_inv_4_task = { #The First City to Fall
		icon = task_conquest

        requires = { nuragic_mission_inv_1_task }

		allow = {
			owns = 3504
		}

		highlight = {
			scope:province = {
				OR = {
					province_id = 3504				
				}
			}
		}


		on_completion = {
			trigger_event = me_nuragic_inv.4
			add_centralization = 10
			add_1_free_province_investments = yes
			add_innovation = 1
		}
	}

	nuragic_mission_inv_5_task = { #Locking the Back Door
		icon = task_religion

        requires = { nuragic_mission_inv_4_task }

		allow = {
			owns = 3506
			p:3506 = {
				fort_level >= 1
				}
		}

		highlight = {
			scope:province = {
				OR = {
					province_id = 3506				
				}
			}
		}

		on_completion = {
			trigger_event = me_nuragic_inv.5
		}
	}

	nuragic_mission_inv_6_task = { #Foothold in Phikaria
		icon = task_conquest

        requires = { nuragic_mission_inv_4_task }

		allow = {
			owns = 3508
			p:3508 = {
				fort_level >= 1
				}
		}

		highlight = {
			scope:province = {
				OR = {
					province_id = 3508				
				}
			}
		}

		on_completion = {
			trigger_event = me_nuragic_inv.6
		}
	}

	nuragic_mission_inv_7_task = { #Eyes on the Hills
		icon = task_expansion

        requires = { nuragic_mission_inv_2_task }

		allow = {
			manpower >= 2
		}


		on_completion = {
			trigger_event = me_nuragic_inv.7
			add_manpower = -2
            add_military_experience = 20
			add_country_modifier = {
				name = "nuragic_scouts"
				duration = 7300
			 
			}
		}
	}

	nuragic_mission_inv_8_task = { #Sacrifice to the Gods of War
		icon = task_battle

        requires = { nuragic_mission_inv_2_task }

		allow = {}

		

		on_completion = {
			trigger_event = me_nuragic_inv.8	

			ordered_owned_province = {
                limit = {
                    dominant_province_culture = nuragic
                }
                order_by = total_population
                max = 1
                ordered_pops_in_province = {
                    limit = { pop_culture = nuragic }
                    order_by = pop_hapiness
                    max = 3
                    kill_pop = yes
					}
				}

			add_country_modifier = {
				name = "nuragic_sacrifice"
				duration = 7300
			}			
		}
	}

	nuragic_mission_inv_9_task = { #The Port of Cornus
		icon = task_happiness

        requires = { nuragic_mission_inv_3_task }

		allow = {
			owns = 3494
		}

		highlight = {
			scope:province = {
				OR = {
					province_id = 3494				
				}
			}
		}

		on_completion = {
			trigger_event = me_nuragic_inv.9
			add_treasury = 100
		}
	}

	nuragic_mission_inv_10_task = { #Sacking of Sulki
		icon = task_battle
		duration = 365

        requires = { nuragic_mission_inv_9_task }

		allow = {
			owns = 3481
		}

		highlight = {
			scope:province = {
				OR = {
					province_id = 3481				
				}
			}
		}

		on_completion = {
			trigger_event = me_nuragic_inv.10
			add_innovation = 1
			add_treasury = 150
			p:3481 = { 
				if = {
					limit = {
						has_city_status = yes
					}
					set_city_status = settlement
				}
			}	
		}
	}

	nuragic_mission_inv_11_task = { #The Farmlands of Karali
		icon = task_political

        requires = { nuragic_mission_inv_10_task }

		allow = {
        	p:3484 = {
        		owner = root
        		num_of_court_building >= 1
        		num_of_port_building >= 1
				num_of_commerce_building >= 1
				num_of_fortress_building >= 1
				num_of_workshop_building >= 1

        		trigger_if = {
        			limit = { has_construction = yes }
        			custom_tooltip = {
        				text = province_has_no_constuction_tooltip
        				has_construction  = no
        			}
        		}
        	}
		}

		highlight = {
			scope:province = {
				OR = {
					province_id = 3484				
				}
			}
		}

		on_completion = {
			trigger_event = me_nuragic_inv.11
			add_centralization = 10
		}
	}

	nuragic_mission_inv_12_task = { #A Southern Capital
		icon = task_atlas
		final = yes

        requires = { nuragic_mission_inv_11_task nuragic_mission_inv_5_task }

		allow = {
			owns = 3484
			has_law = absolute_authority
			political_influence >= 30
			current_ruler = {
				popularity >= 90
			}
		}

		highlight = {
			scope:province = {
				OR = {
					province_id = 3484				
				}
			}
		}

		on_completion = {
			trigger_event = me_nuragic_inv.12
			add_country_modifier = {
				name = "nuragic_power"
				duration = -1
				}
			add_country_modifier = {
				name = "nuragic_archers"
				duration = -1
				}
			set_capital = p:3484
			add_political_influence = -30
			add_2_free_province_investments
			
			
			
		}
	}
}
	nuragic_mission_inv_13_task = { #Seize the Ships
		icon = task_expansion

        requires = { nuragic_mission_inv_4_task }

		allow = {
			treasury >= 80
			political_influence >= 20
		}

		highlight = {
			scope:province = {
				OR = {
					province_id = 3504				
				}
			}
		}

		

		on_completion = {
			trigger_event = me_nuragic_inv.13
			create_unit = {
                navy = yes
                location = 3504
                add_subunit = trireme
                add_subunit = trireme
                add_subunit = trireme
                add_subunit = liburnian
				add_subunit = liburnian
				add_subunit = liburnian
                
            }
		}
	}

	nuragic_mission_inv_14_task = { #A Blind Eye to Piracy
		icon = task_economical

        requires = { nuragic_mission_inv_13_task nuragic_mission_inv_9_task }

		allow = {
        	treasury >= 150
			political_influence >= 30
			
        }
        on_start = {
        	add_political_influence = -30
        	add_treasury = -150
			duration = -1
        }

		highlight = {
			scope:province = {
				OR = {
					province_id = 3494				
				}
			}
		}


		on_completion = {
			trigger_event = me_nuragic_inv.14
			p:3494 = {
				add_permanent_province_modifier = {
							name = pirate_haven_modifier
							duration = -1
						}
				}
		}
	}

	nuragic_mission_inv_16_task = { #Stale Guests
		icon = task_expansion
		duration = 365

		requires = { nuragic_mission_inv_14_task }

		allow = {
			treasury >= 40
			political_influence >= 30
	        }
        
		highlight = {
			scope:province = {
				OR = {
					province_id = 1469				
				}
			}
		}

		on_completion = {
			trigger_event = me_nuragic_inv.16
			add_treasury = -40
			add_political_influence = -30
			declare_war_with_wargoal = {
				war_goal = conquer_wargoal
				province = 1469
				target = c:BLD
			}
		}
	}