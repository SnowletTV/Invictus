colchis_missions = {
    header = "mission_image_general"
    icon = "seleukid_2"                    # CHANGE THESE !!!!!!!

    repeatable = no
    chance = 1000

    potential = {
        NOT = { has_variable = mission_cooldown_var }
        is_subject = no
        has_civil_war = no
		tag = COL
    }

    abort = {
        ai_mission_back_out_trigger = yes
    }
    on_start = {
        trigger_event = me_zhangzhung.26   # CHANGE THIS !!!!!!
        start_mission_ai_effect = yes
    }
    on_abort = {
        custom_tooltip = general_mission_cooldown_tt
        set_variable = {
            name = mission_cooldown_var
            days = 7300
        }
    }
    on_completion = {                         # CHANGE THESE !!!!!!!!!!!!
        complete_mission_effect = yes
        add_2_free_province_investments = yes
        capital_scope = {
            capital_formable_small_effect = yes
        }
    }

    colchis_missions_task_1 = { # Kuji's Gem Fortress
        icon = "task_expansion"
        duration = 90

        allow = {
            p:1715 = {
                has_building = foundry_building
                num_of_fortress_building >= 3
            }
        }
        on_completion = {                    
            area:chomolangma_area = {        # CHANGE
                add_provincial_claim_effect = yes
			}
            add_political_influence = 35
            trigger_event = me_zhangzhung.1       # CHANGE
            show_as_tooltip = {
                p:1715 = {
                    add_permanent_provincial_modifier = {
                        name = kuji_fortress
                    }
                }
            }
        }
    }
    colchis_missions_task_2 = { #Foothills of Terai
        icon = "task_diplomatic"
        duration = 60

        requires = { 01_zhangzhung_missions_task_1}

        highlight = {
			scope:province = {
				OR = {
					province_id = 5573				
				}
			}
		}

        allow = {
            political_influence >= 15
            treasury >= 25
        }
        on_completion = {
            add_political_influence = -15
        	add_treasury = -25
            p:5573 = { set_owned_by = root }
            trigger_event = me_zhangzhung.2
            show_as_tooltip = {

            }
        }
    }
}