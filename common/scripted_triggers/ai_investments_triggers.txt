state_has_permanent_trade_routes = {
    trigger_if = {
        limit = {
            is_capital_state = yes
            owner = {
                has_mid_economic_policy = income_commerce
            }
        }
        trade_routes > 2
    }
    trigger_else = {
        trade_routes > 0
    }
}