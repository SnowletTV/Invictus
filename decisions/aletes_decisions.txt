country_decisions = {

	aletes_secure_loyalty = {
		potential = {
			has_variable = aletes_apotheosis_progress
			char:558 = {
				is_alive = yes
				employer = root
			}
		}
		allow = {
			custom_tooltip = {
				text = aletes_has_enough_prestige_points_tooltip
				var:aletes_apotheosis_progress > 10
			}
			OR = {
				custom_tooltip = {
					text = aletes_married_into_ruling_family
					var:aletes_wife_family_flag = { any_family_member = { is_ruler = yes } }
				}
				hidden:root.var:aletes_flag = {
					is_friend = root.current_ruler
				}
			}
			hidden:var:aletes_flag = {
				custom_tooltip = {
					text = aletes_is_loyal
					loyalty >= aletes_is_loyal_loyalty_total
				}
			}
		}
		effect = {
			custom_tooltip = aletes_apotheosis_status_tooltip
			aletes_apotheosis_status = yes
			custom_tooltip = aletes_loyalty_secured_tooltip
			aletes_adventure_main_variable_cleanup_effect = yes
			aletes_adventure_event_variable_cleanup_effect = yes
			set_variable = aletes_apotheosis_secured
		}
		ai_will_do = {
			base = 100
		}
	}
	
	aletes_banish_decision = {
		potential = {
			has_variable = aletes_apotheosis_progress
			char:558 = {
				is_alive = yes
				employer = root
			}
		}
		allow = {
			hidden:root.var:aletes_flag = {
				is_rival = root.current_ruler
			}
			hidden:var:aletes_flag = {
				custom_tooltip = {
					text = aletes_is_disloyal
					loyalty <= aletes_is_disloyal_loyalty_total
				}
			}
		}
		effect = {
			custom_tooltip = aletes_apotheosis_status_tooltip
			aletes_apotheosis_status = yes
			if = {
				limit = {
					var:aletes_apotheosis_progress >= 12
					NOT = { tag = MAT }
				}
				custom_tooltip = aletes_will_rebel_independence_tooltip
				trigger_event = {
					id = flavor_mat.64
				}
			}
			if = {
				limit = {
					var:aletes_apotheosis_progress >= 12
					tag = MAT
				}
				custom_tooltip = aletes_will_rebel_tooltip
				trigger_event = {
					id = flavor_mat.65
				}
			}
			else = {
				custom_tooltip = aletes_adventure_forced_ending_tooltip
				char:558 = {
					banish = char:558
				}
				aletes_adventure_main_variable_cleanup_effect = yes
				aletes_adventure_event_variable_cleanup_effect = yes
			}
		}
		ai_will_do = {
			base = 0
		}
	}
	
}