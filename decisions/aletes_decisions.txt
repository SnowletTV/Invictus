country_decisions = {

	aletes_secure_loyalty = {
		potential = {
			has_variable = aletes_apotheosis_progress
			char:553 = {
				is_alive = yes
				employer = root
			}
			NOT = { has_variable = aletes_apotheosis_secured }
		}
		allow = {
			custom_tooltip = {
				text = aletes_has_enough_prestige_points_tooltip
				var:aletes_apotheosis_progress > 10
			}
			hidden:current_ruler = {
				is_friend = char:553 
			}
			char:553 = {
				loyalty >= 80
			}
		}
		effect = {
			custom_tooltip = aletes_apotheosis_status_tooltip
			aletes_apotheosis_status = yes
			custom_tooltip = aletes_will_be_deified_tooltip
			remove_variable = aletes_apotheosis_progress
			set_variable = aletes_apotheosis_secured
		}
		ai_will_do = {
			base = 100
		}
	}
	
	aletes_banish_decision = {
		potential = {
			has_variable = aletes_apotheosis_progress
			char:553 = {
				is_alive = yes
				employer = root
			}
			NOT = { has_variable = aletes_apotheosis_secured }
		}
		allow = {
			current_ruler = {
				is_rival = char:553 
			}
			char:553 = {
				loyalty <= 30
			}
		}
		effect = {
			custom_tooltip = aletes_apotheosis_status_tooltip
			aletes_apotheosis_status = yes
			if = {
				limit = {
					var:aletes_apotheosis_progress >= 12
					tag = MAT
				}
				custom_tooltip = aletes_will_rebel_tooltip
				trigger_event = {
					id = flavor_mat.51
				}
			}
			if = {
				limit = {
					var:aletes_apotheosis_progress >= 12
					NOT = { tag = MAT }
				}
				custom_tooltip = aletes_will_rebel_tooltip
				trigger_event = {
					id = flavor_mat.50
				}
			}
			else = {
				char:553 = {
					banish = char:553
				}
				remove_variable = aletes_apotheosis_progress
			}
		}
		ai_will_do = {
			base = 0
		}
	}
	
}