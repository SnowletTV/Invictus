country_decisions = {

	dahae_winwar = {
		potential = {
			has_variable = dahae_defender_whitepeace
			any_current_war = {
				any_war_participant = {
					this = ROOT
				}
			}
		}

		allow = { 
			any_current_war = {
				is_war_leader = ROOT
				war_score = {
					target = ROOT
					value >= 10
				}
			}
		}

		effect = {
			remove_variable = dahae_defender_whitepeace
			custom_tooltip = "dahae_winwar.tt"
			every_current_war = {
#				limit = {
#					any_war_participant = {
#						AND = {
#							THIS = scope:dahae_aggressor_target
#							THIS = ROOT
#						}
#					}
#				}
				force_white_peace = THIS
			}
		}
	}
	
	dahae_jv = {
		potential = {
			has_variable = dahae_diplo
		}
		
		allow = {
			treasury >= 30
			any_allied_country = {
				OR = {
					tag = PRN
					tag = ZNT
					tag = PSS
					tag = SA4
					tag = SCA
				}
			}
		}
		
		effect = {
			custom_tooltip = "dahae_investment.tt"
			add_treasury = -30
			trigger_event = {
				id = me_dahae.27
			}
			set_variable = dahae_invested
			random_allied_country = {
				limit = { is_in_list = dahae_countries }
				save_scope_as = dahae_jv1
			}
		}
	}
	
	parthia_debug_leftside = { #TODO DELETE
	
		potential = {
			tag = ZNT
		}
		
		allow = {}
		
		effect = {
			set_variable = parthia_decentralized_wg
		}
	}
	
	parthia_debug_rightside = { #TODO DELETE
	
		potential = {
			tag = ZNT
		}
		
		allow = {}
		
		effect = {
			set_variable = parthia_centralized_wg
		}
	}
}

