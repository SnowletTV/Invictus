country_decisions = {
	
	form_greater_media = { #Shouldn't be formed by anyone but Media
		
		potential = {
			num_of_cities >= 1
			tag = MEE
			NOT = {
				tag = GME
			}			
		}
		
		highlight = {
			scope:province = {
				OR = {
					is_in_region = cappadocia_pontica_region
					is_in_region = armenia_region
					is_in_region = media_region
					is_in_region = parthia_region
					is_in_region = ariana_region
            		is_in_region = persis_region
            		is_in_area  = garamig_area
				}
			}
		}
		
		allow = {
			custom_tooltip = {
				text = greater_media_not_exists
				NOT = {
					any_country = {
						tag = GME
					}
				}
			}
			is_subject = no
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = cappadocia_pontica_region
			owns_or_subject_owns_region = armenia_region
			owns_or_subject_owns_region = media_region
			owns_or_subject_owns_region = parthia_region
			owns_or_subject_owns_region = ariana_region
			owns_or_subject_owns_region = persis_region
			owns_or_subject_owns_area = garamig_area
		}
		
		
		effect = {
			custom_tooltip = "tier_3_formable_tooltip"
			change_country_name = "GREATER_MEDIA_NAME"
			hidden_effect = {
				#change_country_adjective = "GREATER_MEDIA_ADJECTIVE" don't think I need? should be same as Media
				change_country_tag = GME
				#change_country_flag = BHARAT_FLAG # Either gonna make a new flag for Media which can be picked, or for Greater Media
				trigger_event = {
					id = me_atropatene.46
				}
			}
			add_5_free_province_investments = yes
			add_country_modifier = {
				name = formable_country_huge_modifier
				duration = 5475
			}
		}
		
		ai_will_do = {
			base = 1
		}
	}

}

