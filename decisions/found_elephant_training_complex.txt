country_decisions = {
	
	# Found Elephant Training Complex
	found_elephant_training_complex = {
	
		potential = {
			num_of_cities >= 1
			owns = 7587
			NOR = {
				has_variable = founded_elephant_training_complex
			}
			OR = { 	
				country_culture_group = meroitic_group
				country_culture_group = aksumite_group
				any_country_culture = {
					is_integrated = yes
					num_pops_same_group_decisions >= percent_size_culture_group_comparison
					is_old_egyptian_culture_group_trigger = yes
				}			
			}
		}
		highlight = {
			scope:province = {
				province_id = 7587
			}
		}
		allow = {
			treasury >= 100
			military_experience >= 65
			capital_scope = { civilization_value >= 30 }
			OR = {
				AND = {
					centralization >= 50
					is_tribal = yes
				}
				is_monarchy = yes
				is_republic = yes
			}
			owns = 7587
		}
		
		effect = {
			set_variable = founded_elephant_training_complex
			add_treasury = -100
			add_military_experience = -65
			add_country_modifier = {
				name = elephant_training_programme 
				duration = -1
			}			
			p:7587 = {
				add_province_modifier = {
					name = elephant_training_complex
					duration = -1					
				}
			}		
		}
		
		ai_will_do = {
			base = 100
		}
	}
}
