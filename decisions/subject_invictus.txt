country_decisions = {
	
	overlord_conquer_please = {
		
		potential = {
			is_subject = yes
			is_ai = no
		}

		allow = {
			always = yes
		}
		
		effect = {
			declare_war_with_wargoal = {
				war_goal = conquer_wargoal
				province = p:4809
				target = c:GTE
			}
		}
	}

	overlord_join_please = {
		
		potential = {
			is_subject = yes
			is_ai = no
		}

		allow = {
			always = yes
		}
		
		effect = {
			random_current_war = {
				limit = {
					any_war_participant = {
						this = root
					}
					any_war_participant = {
						this = c:GTE
					}
					is_war_leader = root
				}
				save_scope_as = random_war_scope
			}
			overlord = {
				add_to_war = {
					target = scope:random_war_scope
					attacker = yes
					leader = no
				}
			}
		}
	}
}

