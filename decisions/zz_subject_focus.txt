country_decisions = {
	
	#Subject Focus for 2-3 subjects
	subject_focus_small = {
	
		potential = {
			OR = {
			tag = KSH
			tag = EGY
			}
			OR = {
			any_subject = {
        	count = 2
            }
			any_subject = {
        	count = 3
            }
            }            
            NOT = {
            has_variable = subject_focus_cooldown
            }
		}
		
		allow = {
			political_influence >= 50
		}
		
		effect = {
			set_variable = {
			name = subject_focus_cooldown
			days = 7300 #5 years
			value = 1
			}
			add_political_influence = -50
        	trigger_event = { id = me_subject_focus.1 }			
		}
		
		ai_will_do = {
			base = 0
		}
	}
	#Subject Focus for More 4 or more subjects.
	subject_focus_large = {
	
		potential = {
			OR = {
			tag = KSH
			tag = EGY
			}		
			any_subject = {
        	count >= 4
            }	            
            NOT = {
            has_variable = subject_focus_large_cooldown
            }
		}
		
		allow = {
			political_influence >= 100
		}
		
		effect = {
			set_variable = {
			name = subject_focus_large_cooldown
			days = 7300 #20 years
			value = 1
			}
			add_political_influence = -100
        	#trigger_event = { id = me_subject_focus.1 }			
		}
		
		ai_will_do = {
			base = 0
		}
	}

}