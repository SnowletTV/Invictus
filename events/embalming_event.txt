namespace = embalming

embalming.1 = {
	type = country_event
	hidden = yes

	trigger = {
		invention = embalming_inv
		}
		OR = {
			country_culture_group = north_african
			tag = EGY
		}
	}

	immediate = {
		set_variable = {
			name = mummy_candidate
			days = 180
		}
	}
}
